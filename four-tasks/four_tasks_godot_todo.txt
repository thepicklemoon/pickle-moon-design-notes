FOUR TASKS — Godot Todo
========================
Last edit: 2026-05-26 AWST
Last updated: session 18 close — tile 2.9 DevKit landed. Morning sequence collision still flagged as content-drop blocker.

Forward-looking tile list organised by phase. Work topmost incomplete tile in current phase; drop down rather than jumping phases.

Completed-tile history with session attribution + time logged: devlog v2 SUMMARY block + v1 archive.

Tags: [CODE] [ART] [CONFIG] [ADMIN] [DECIDE]
Status: [ ] not started, [~] in progress / design closed, [x] done
Convention: ticked tiles get `Time: ~Xh` or `~Xmin` on title line.
Tiers (for items outside phase structure): ACTIVE / PARKED / DEFERRED.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
APPTRIOC FORK ARCHITECTURE (locked session 5)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

APPtrioc is a snapshot of Four Tasks at the end of tile 4.14a (pre-long-press picker), exported to HTML5, deployed to Cloudflare Pages, configured for unilateral pairing + atrioc-themed stickers + three-button onboarding. Separate Worker + D1.

Fork sits between 4.14a (basic picker, tap-to-toggle pool) and 4.14b (long-press context menu, theme manager). APPtrioc never gets post-fork tiles — they're the conversion pull.

Launch sequence: Four Tasks ships first (Phase 5), then APPtrioc (Phase 5b), then atrioc video + Reddit post drops. atrioc moment is force multiplier on shipped product, not launch driver. See `four_tasks_marketing_notes.md`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 0 — Pre-flight (CLOSED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] 0.1 [CONFIG] Godot project created. Session 1. ~1h.
[x] 0.2 [CONFIG] Project settings (1080×2400, Nearest, portrait). Session 1. ~1h.
[x] 0.3 [DECIDE] Fonts imported (Departure Mono + JetBrains Mono); body font deferred to Phase 2/3. Session 1. ~1h.
[x] 0.4 [CONFIG] Devkit skeleton (ungated until tile 5.2). Session 1. ~30min.
[x] 0.5 [ADMIN] ABN + accountant. Session 1. ~1.5h.
[x] 0.6 [ADMIN] myGovID + RAM. Session 1. ~25min.
[x] 0.7 [ADMIN] Business name THE PICKLE MOON registered ($104, 3-year). Session 1. ~20min.
[x] 0.8 [ADMIN] Mac access deferred to Phase 5. Session 1. ~10min.
[x] 0.9 [DECIDE] Monetisation locked. See `four_tasks_monetisation_position.md`. Session 1. ~1.5h.
[x] 0.10 [ADMIN] Privacy policy v7 in `pickle-moon-public`. Session 1. ~30min.
[x] 0.11 [DECIDE/CONFIG] Ledger live (`pickle-moon-ledger`). Session 1+2. ~1.25h.
[x] 0.12 [CONFIG] Dev environment. Session 2. ~15min.

GATE: Closed. Project opens, ABN active, business name registered, toolchain ready, privacy policy + ledger live.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1 — Backend + foundations
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] 1.0 [CONFIG] Cloudflare account. Session 1. ~10min.
[x] 1.0a [DECIDE/ADMIN] Domain `thepicklemoon.com` registered. Session 1. ~30min.
[x] 1.0b [CONFIG] Toolchain (Node v24.15.0 + Wrangler 4.90.0 + OAuth). Session 1. ~25min.
[x] 1.1 [CODE] Worker scaffold + D1 schema. Session 2. ~3h.

[x] 1.2 [CODE] Worker endpoints v1. Session 3+12. ~3h initial + rewritten in session 12.
    GET /health, GET /users/:user_id, POST /resolve. Wholesale rewrite to user_id model in session 12.

[x] 1.3 [CODE] Worker defensive write rules. Session 12. ~5h.
    Ten write endpoints across three batches:
      Batch A: POST /users, POST /users/:user_id/bug_report, POST /users/:user_id/dismiss_unpair_notice, PUT /users/:user_id/days/:date.
      Batch B: PUT /users/:user_id (with rotation), POST /users/:user_id/unpair, POST /users/:user_id/join_by_values.
      Batch C: POST /users/:user_id/claim (lazy seal + payout + stamp + streak + theme snapshot).
    v1.0 schema rewritten wholesale (7 tables, 36 cols). Old migration_001/002/003.sql DELETED. schema.sql is single source of truth. Partner reactions killed for v1.0. Promo codes scope added. morning_payout_due_for column dropped (redundant under lazy-seal).
    All 11 endpoints curl-verified locally + deployed remote. Stub stamp_messages.ts created.

[x] 1.4 [CODE] Lazy seal-on-open. ABSORBED into tile 1.3 claim endpoint transaction. Session 12.

[x] 1.5 [CODE] Godot Backend autoload. Stateless HTTP layer, 22 signals (success+failure per endpoint). Spawn-per-call HTTPRequest. Time: ~30min.

[x] 1.6 [CODE] Godot State autoload. In-memory mirror. Days re-indexed for O(1) lookup. Optimistic mutation + snapshot rollback. UI-lockout via _writes_in_flight. Time: ~30min.

[x] 1.7 [CODE] Godot Identity autoload. user://identity.cfg via ConfigFile. Emits boot_resolved(has_identity, load_succeeded) for the boot router. Time: ~30min.

GATE: Closed. Cold-boot manually-populated identity.cfg loads pair data from Worker. Three boot paths verified.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2 — Visible app skeleton
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] 2.1 [CODE] Loader scene. Centred Pickle Moon splash + hidden ErrorLabel. Routes to App / Onboarding / retry-on-error via Identity.boot_resolved. Scene swaps via call_deferred. Throwaway app.tscn + onboarding.tscn stubs. Time: ~1h.

[x] 2.2 [CODE] App scene with two-panel swipe. PanelYou + PanelPartner. 8px intent-lock, 1:1 drag with 50px edge give, always-snap at 12% threshold, 400ms TRANS_QUART EASE_OUT. Mouse + touch. Time: ~1h.
[x] 2.3 [CODE] Calendar grid. `calendar_grid.tscn` + `day_cell.tscn`. Reads `State.days`. 9-state cell model (UNMARKED/RED/ORANGE/YELLOW/GREEN/REST + DEAD_HISTORICAL/PRE_INSTALL/FUTURE). Prev/next month nav bounded by install_iso (left) and today (right). Off-month cells render real prev/next dates dead-treated. Always 6 rows. Sessions 13+14+15. ~5h total.
[x] 2.4 [CODE] Task list. `task_list.tscn` + `task_row.tscn`. Reads State. Tap toggles via State.toggle_task (no in-flight guard, no rollback). Past days render as post-payout (done = green ✓, undone = red ✕). Rest days = all rows purple ✓. Partner tray read-only. Session 15. ~2h.
[x] 2.5 [CODE] Tasks tray. `tasks_tray.tscn` per panel. Slide-down on day-cell tap, slide-up on close ✕ or tap-same-day. Day-swap = close + open new. Cross-panel day-carry on swipe. Session 15. ~1.5h.
[x] 2.6 [CODE] Streak bar — self panel. Flame + current_streak + coin pill in single rounded bar. Merged with 2.7 (same visual unit). Session 16. ~2h combined.
[x] 2.7 [CODE] Bonus pill — partner panel. Purple capsule showing partner-yesterday-derived multiplier. Bonus.gd autoload computes; client-side rule mirrors the future server payout rule. Session 16. ~2h combined.
[x] 2.8 [CODE] Partner panel polling. 30s foreground / paused background / 10s deferred focus-in. Backend.poll_user() + State selective-merge (in-flight write preservation). Verified end-to-end via curl. Session 16. ~1.5h.
[x] 2.9 [CODE] DevKit. Scenario menu overlaid on live app, separate dev worker + D1, build-flag-gated (editor OR custom feature). Three live scenarios (default / fresh_install / recovery_flow); ~15 more stubbed. See `four_tasks_devkit_design_notes.md`. Session 18. ~1h.

GATE: Boot with seeded pair, see tasks, tick them, see saves server-side, switch panels, see partner data, reboot to onboarding. Bones present.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3 — Onboarding + identity flow
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Reference: `four_tasks_onboarding_design_notes.md`. Solo-as-default, 8-screen flow, pairing post-onboarding via "Add my partner" button on partner panel. Single /join_by_values endpoint.

PENDING:
  - Four-tasks entry screen placement (copy locked, slot TBD between 6+7 or 7+8).
  - "Start the day" button on screen 8 — fire day-1 morning sequence immediately or defer?

VALIDATION RULES (every name/username/sticker input):
  - No `|`, no `__`, non-empty.
  - UX caps: username ~20 chars (server 40), sticker ID ~shorter (server 50), task labels ~30 (server 49).

[ ] 3.1 [CODE] Onboarding scene state machine + entry routing (fresh install / post-onboarding pair / recovery).
[ ] 3.2 [CODE] Welcome (1) + first-time/returning fork (1a). Primary [First time here] + smaller "Recover my calendar".
[ ] 3.3 [CODE] Name (2) + immutable confirmation modal (3). Forward-references username step.
[ ] 3.4 [CODE] Username (4). Mutable.
[ ] 3.5 [CODE] Starting avatar (5) + theme assembly theatre + themed companion pool popup (5b). Curated launch starter subset. ~2s theatre (glitch typewriter + bounce-twist staggered spawn). 5b introduces per-day random celebration sticker + 7-day strip.
[ ] 3.6 [CODE] MOTD (6). Four spinners. Tutorial coins (capped 750, "More coins" arming button below 70-110 reroll threshold). Tutorial coins discarded on transition.
[ ] 3.7 [CODE] Partner's calendar (7). Swipe-right prompt. Persistent "Add my partner" button. Library sharing soft-plant.
[ ] 3.8 [CODE] Coins and picker, closing (8). Coin blitz animation (2-3 stickers). [Start the day] button.
[ ] 3.9 [CODE] Four-tasks entry screen. Copy locked. Placement TBD.
[ ] 3.10 [CODE] Post-onboarding pairing — "Add my partner" + /join_by_values endpoint integration. Server returns 409 on ambiguous; client surfaces "vary username temporarily" workflow. Recovery flow (six-value entry) lives here too.

GATE: Fresh install walks welcome → first day. Solo user on App scene with own data. Pairing works for ambiguous + unambiguous matches.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4 — Look, feel, hero moments
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Fork between 4.14a (pre-fork, APPtrioc inherits) and 4.14b (post-fork, Four Tasks exclusive).

━━━ PRE-FORK CODE TILES (APPtrioc inherits) ━━━

[ ] 4.1 [CODE] Glitch typewriter utility (typewriter + glitch_all_marked).
[ ] 4.2 [CODE] Wire glitch-word reveals into onboarding popups. 24 marked words from web build.
[ ] 4.3 [CODE] CoinFX utility — fly, count-up, blitz, flash, particle.
[ ] 4.4 [CODE] Slot machine — real wheels. Four spinners. Sticker wheel reads Lever 1 pool. FIX: true independent randomisation per slot (prototype alternates visibly).
[ ] 4.5 [CODE] MOTD reroll cost. First free during sequence; subsequent flat 90-110 coins, no escalation. Reroll is VENT not sink. Lock button + confirm popup.
[ ] 4.6 [CODE] Morning sequence — full coordinator. All 12 beats. Claim endpoint fires at beat 3. Reference: `four_tasks_morning_sequence_design_notes.md`. Required reading before this tile.
[ ] 4.7 [CODE] Stamp slap animation. Tier colours red/orange/yellow/green/purple. Authoring 10-20 messages per tier (stubs in stamp_messages.ts currently).
[ ] 4.8 [CODE] Rest day system. Long-press future cell, confirm, refund. Same morning sequence with content swaps. Author rest-day task label pool (~15-30).
[ ] 4.9 [CODE] Streak popup with confetti. Milestone bonuses every 5 days.
[ ] 4.10 [CODE] Month stamp + month stats popup.
[ ] 4.11 [CODE] Help menu — two-tier. Six subjects, [g]glitch[/g] markup.
[ ] 4.12 [CODE] Bug catcher — popup, submit, confetti, thanks.
[ ] 4.13a [CODE] Leaderboard endpoint — GET /leaderboard. DEFERRED (`four-tasks/deferred/`). Possibly not shipping.
[ ] 4.13 [CODE] Leaderboard UI. Depends on 4.13a revival.
[ ] 4.14a [CODE] Basic sticker picker — tap-to-toggle pool membership ONLY. Floor of 1. Themed-sticker marker rendered (conversion pull for APPtrioc).
[ ] 4.D2 [CODE] Week mode. Per-weekday opt-in toggle (off by default). Divergence on first task edit, not on toggle. Cal-icon stays universal standard-four editor with warning on week-mode days. Today-only edit rule. Personal not shared. Schema: `week_mode_weekdays` bitmask + `user_weekday_overrides` table (already in v1.0 schema). Reference: `four_tasks_week_mode_design_notes.md`.

━━━ PRE-FORK ART TILES ━━━

[ ] 4.A [ART] Coin sprite.
[ ] 4.B [ART] Frog mascot.
[ ] 4.C [ART] Stamp set. Five+ stamps with variants.
[ ] 4.D [ART] Sticker catalogue. Folder per sticker, `<id>_<role>.png` naming. Palette derived from sticker.png pixel-frequency (≤3 non-grey colours). 32×32 RGBA. Sticker.png is the EXCEPTION to palette-agnostic art rule (real colours; all else in canonical reference values, retinted at runtime). First frog painted (session 8, ongoing in `pickle-moon-assets`). Painting continues at FIFO/laptop time. Reference: `four_tasks_theme_design_notes.md`.
[ ] 4.E [ART] UI chrome (buttons, popups, dividers, util bar). Themed surfaces in reference palette; sacred-semantic in final colours.
[ ] 4.F [ART] Onboarding example calendar screenshots. Real Godot build screenshots once theming in.

━━━ FORK POINT ━━━ APPtrioc launches from snapshot. See PHASE 5b. ━━━

━━━ POST-FORK CODE TILES (Four Tasks exclusive) ━━━

[ ] 4.14b [CODE] Sticker picker context menu + theme manager. Long-press (500ms + vibration) on picker. Unlocked sticker menu: per-slot icons, tap = instant application + APPLY ALL. Locked sticker menu: PREVIEW button (temporary, ~30s revert). Pair-key migration fires on `active_leader` change ONLY.
    BLOCKERS: theme implementation prep items (see PARKED). Reference: `four_tasks_theme_design_notes.md`.
[ ] 4.15 [CODE] Username + icon edit. Settings → triggers rotation. "Changes may take a minute" dismissible popup.
[ ] 4.16 [CODE] Partner reactions. DEFERRED to v1.x. Doc moved to `four-tasks/deferred/`.
[ ] 4.17 [CODE] Coin name generator (server-side). DEFERRED.
[ ] 4.18 [CODE] Coin name reroll UI + endpoint. DEFERRED (depends on 4.17).
[ ] 4.19 [CODE] TutorialCoordinator autoload + reveal content. v1.x scope. Reads `users.tutorial_progress`, computes day-of-use, fires one reveal per session boot. Day-2 long-press philosophy reveal uses gesture-dismissal (no "Got it" button). Reference: `four_tasks_staggered_disclosure_design_notes.md`.
[ ] 4.20 [CODE] Palette derivation devtool. Loads sticker.png, displays derived palette swatches with RGB + pixel counts. Read-only. Grey-exclusion rule MUST match runtime renderer (shared function). Lands as a DevKit scenario / sub-screen rather than a separate scene.
[ ] 4.21 [CODE] Un-pair flow — symmetric break + change-username follow-up + dismissable banner. Time: ~3-4h.
    Server: POST /users/:user_id/unpair already implemented (tile 1.3). Dismiss endpoint already implemented.
    Client A (initiator): long-press partner panel header → menu → `Un-pair`. Popup: title `Un-pair?`, no body, Cancel/Un-pair (mild red tint). On 200, change-username follow-up popup: `Change your username?`, pre-filled input, Skip/Save.
    Client B (recipient): sees banner `[username] un-paired.` with × dismiss. Position TBD at implementation. One-shot, server-tracked.
    Post-un-pair state = post-onboarding-solo state. Banner is only delta on B's side. No cooldown. Re-pair via standard `Add my partner`. UI always says "un-pair". Reference: `four_tasks_pair_key_design_notes.md` Section 17.

GATE: App looks like Four Tasks. Theming changes via picker context menu. Onboarding feels right. Fork point passed — APPtrioc shipped (Phase 5b), Four Tasks continued through post-fork depth.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5 — Ship Four Tasks to stores
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

>>> BUY A FUCKIN MAC <<<
(Mac decision deferred from tile 0.8. Options: M4/M5 mini, MacInCloud, borrow. Needed before tile 5.5.)

[ ] 5.1 [CONFIG] Production Worker deployment. Real domain (`api.fourtasks.thepicklemoon.com`). Builds point to production; dev/test to staging.
[ ] 5.2 [CONFIG] DevKit excluded from release exports — verify "devkit" custom feature is absent from store export presets (Project → Export → preset → Resources → Custom Features). Build-flag plumbing already done in tile 2.9; this tile is the audit at Phase 5 gate.
[ ] 5.3 [CODE] Production logging + error reporting.
[ ] 5.4 [CONFIG] Android build — release signing, keystore, signed `.aab`, verify on Pixel.
[ ] 5.5 [CONFIG] iOS build — Xcode export, signing, archive, TestFlight, install on iOS device.
[ ] 5.6 [ART] App icon master 512×512.
[ ] 5.7 [ART] Store screenshots. 4-6 portraits per store.
[ ] 5.8 [ART] Feature graphic Google Play. 1024×500.
[ ] 5.9 [ADMIN] Privacy policy finalised + hosted. Final pass on `privacy_policy.md`. Flip repo public OR Cloudflare Pages from private. Align with honest threat model (pair-key Section 9).
[ ] 5.9a [ADMIN] Terms of Service drafted + hosted. Optional for submission, worth having before paying users. Plain-language, matches privacy policy tone. ~1-2h. Lives in `pickle-moon-public/terms.md`.
[ ] 5.10 [ADMIN] Google Play Console setup.
[ ] 5.11 [ADMIN] Apple App Store Connect setup.
[ ] 5.12 [ADMIN] Google review submission.
[ ] 5.13 [ADMIN] Apple review submission. Reviewer notes: "use pair name testa / partner testb to bypass onboarding."
[ ] 5.14 [ADMIN] Address review feedback.
[ ] 5.15 [ADMIN] Quiet launch via subculture seeding (Reddit/Discord). atrioc video held until 5b completes.

GATE: Two store listings, two live apps. Quiet launch. APPtrioc not yet live.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5b — APPtrioc fork + ship
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] 5b.0 [DECIDE] APPtrioc naming sanity check. Quick research on atrioc precedent for branded use. If clean → ship as APPtrioc. If hesitant → DM permission first.
[ ] 5b.1 [CODE] Snapshot Four Tasks at fork point. Branch `apptrioc-fork-base` off `four-tasks` repo at commit after 4.14a closes.
[ ] 5b.2 [CONFIG] Create `pickle-moon-apptrioc` repo. Private to start.
[ ] 5b.3 [CODE] APPtrioc backend — separate Worker + D1. Simpler schema (unilateral, ~5-6 endpoints). Crib Four Tasks conventions. Zero shared code. User can't exist in both.
[ ] 5b.4 [ART] APPtrioc sticker set + theme art. 5-8 hand-painted riffing on atrioc community.
[ ] 5b.5 [CODE] APPtrioc skin + config on fork branch. Three-button onboarding (atrioc / Ari / everyone else) with password gates for first two. Default partner = atrioc for "everyone else". Tooltip: "want themes? Four Tasks has more."
[ ] 5b.6 [CODE] Atrioc chain-import sweetener. One-time data import; atrioc on first login sees existing don't-break-the-chain calendar from public stream data.
[ ] 5b.7 [CONFIG] Godot HTML5 export pipeline. PWA manifest + service worker. Browser-only, not on stores.
[ ] 5b.8 [CONFIG] Deploy APPtrioc to Cloudflare Pages. `apptrioc.thepicklemoon.com`. Browser + mobile test.
[ ] 5b.9 [ADMIN] Leak atrioc password 1-2 weeks pre-video. Hidden in Twitch chat / YouTube description. Leak Ari's separately.
[ ] 5b.10 [ADMIN] Record launch video. 10min, self-shot, voice-over. Hard 20h edit budget. No licensed music. Filmed after APPtrioc live.
[ ] 5b.11 [ADMIN] Launch — video + Reddit post + APPtrioc live, same day.

GATE: APPtrioc live, video dropped, Reddit post up. Both products in market.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ACTIVE — ad-hoc items found during the port
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] Morning sequence beat collision resolution. Priority hierarchy across payout / staggered disclosure / content drops / urgent comms / single-fire comms; per-morning popup cap; per-user per-beat "shown" tracking. Update `four_tasks_morning_sequence_design_notes.md`. Blocks content drops shipping in production.

[ ] On-touch jitter tuning pass. Mouse testing makes the 8px DRAG_DEADZONE hard to evaluate. Tap-with-finger-jitter behaviour needs phone test. Constants `DRAG_DEADZONE` (App.gd) and `TAP_DRAG_THRESHOLD` (Layout.gd, used by TaskRow + DayCell) may need separate values for touch. Defer until first Android run.

[ ] Group / triple expansion design conversation. NOT v1.0, NOT v1.x, possibly never. Open: star vs full mesh, pair_key extension for groups, UX (swipe breaks at three calendars), monetisation hook. See pair-key Section 17.

[ ] Doc sync pass (session 12 + 14 batch):
    - Partner reactions design doc → MOVE to `four-tasks/deferred/`.
    - `pickle_moon_repo_index.md`: partner reactions out of Locked → into deferred; promo codes new entry pending design doc; schema.sql migration-files-deleted note; write_rules doc moved Locked → Background/Superseded.
    - Project system prompt: drop "first feature with field-level write rules" reference.
    - Terminology updates across design docs: `diary` → `motd`, `emoji` → `active_leader`.
    - `four_tasks_timezone_and_sealing_design_notes.md`: add boot-only sync model note.
    - `four_tasks_morning_sequence_design_notes.md`: MOTD framing (MOTD in onboarding per session 9). "Migration window" → "rotation window" terminology.
    - `four_tasks_theme_design_notes.md`: `<id>_<role>.png` naming convention update; trigger-gated picker hint replacing day-7 scheduled. Z-order pipeline layer 3 says "stamp art (when claimed)" — actually means STICKER, not stamp. Cell stack is: bg → date/pips → sticker → theme overlay → (no stamp layer). Stamp lives on the tray, not the cell.
    - `four_tasks_week_mode_design_notes.md`: status v1.x → v1.0.
    - `four_tasks_stamp_tier_design_notes.md`: obsolete "nightly cron, tile 1.4" reference.
    - `four_tasks_onboarding_design_notes.md`: name confirmation modal displays typed value EXACTLY as typed (case-sensitive per session 10).
    - Privacy policy: align with honest threat model from pair-key Section 9.
    - `pickle_moon_repo_index.md` + privacy policy: schema gained `users.created_at` (session 14) — symmetric with `pairs.created_at`, drives calendar floor.

[ ] Promo codes design doc. Schema landed in session 12 against ad-hoc decisions. Formalise as `four_tasks_promo_codes_design_notes.md` before subreddit outreach. Captures: single-code-per-user enforcement, code shape (FIFOFOUNDER20-style), founders_rate permanent once granted, redemption_attempts logs all attempts including failures.

[ ] identity.cfg backup hook — feasible with stable user_id (session 10). iOS Keychain / Android Keystore / iCloud / Google Drive could restore user_id on device replacement. NOT v1.0-blocking. Worth a focused conversation in Phase 4/5.

[ ] Update devlog AI Reading Guide for repo-fetching workflow. Current "ask Morgan to upload files" instruction is stale. Rewrite: "Ask Morgan which docs changed; fetch via raw GitHub URL using `pickle_moon_repo_index.md` as canonical map." Small laptop edit.

[ ] Log Cloudflare domain receipt in ledger. `thepicklemoon.com` paid 11 May 2026, invoice IN-64839053, USD $10.46. AUD from card statement (~A$16-17). Files: `pickle-moon-ledger/receipts/2026-05-11_cloudflare_domain-registration_invoice.pdf` + `_paid-status.png`. Category: software_subscription. Renewal 10 May 2027.

[~] Marketing notes — running document. Ongoing journal entries.

[ ] Theme system implementation prep — remaining dimensions + retint pass. Session 8 progress: sticker canvas 32×32 locked, palette via sticker.png pixel-frequency. Open items: cell sticker render size (~100px), picker grid render size (~200px), cell variant source sizes, grid tile size, background slot dimensions, flourish + label bounding boxes, safe-area rules, retint mechanism choice (shader/pre-bake/indexed PNG), reference palette RGB values, default theme art design, themed-sticker marker style, context-menu icon vocabulary, `effects_active.tres` + `effects_ambient.tres` schemas, `theme_audio.tres` schema, effects coordinator architecture, audio playback architecture, locked-vs-unlocked picker rendering, preview timeout duration, exact "grey" definition for palette exclusion. Focused design conversation with Aseprite open. Blocks 4.14b, not 4.D.

[ ] APPtrioc backend prep — focused design pass before 5b.3. APPtrioc D1 schema, atrioc/Ari special-case bilateral pairing inside unilateral model, password-gate flow, endpoint surface + write rules per endpoint, rate limiting. Own design doc in `pickle-moon-apptrioc` when repo exists.

[ ] APPtrioc-to-Four-Tasks keepsake export. Downloadable keepsake image on request when migrating. Low engineering, high goodwill. Part of Phase 5b or post-launch.

[ ] Year-end keepsake export. Calendar-year cap means old days stop being returned client-side on Jan 1 but stay in DB. New endpoint `GET /pair/:key/year/:year`. Image composed client-side. First fires Jan 1 2027. Launch by July 2026 latest so users have months of data.

[ ] Rate limiting — Cloudflare Workers Rate Limiting API, per-IP at platform edge. Generous limits (real users hitting bugs may legitimately file three reports in five minutes). Applies to all write endpoints + APPtrioc. Own design conversation after a few more endpoints exist. Not blocking 1.3.

[ ] IANA timezone conversion (calendar floor + today). `State.install_iso()` and `State.today_iso()` currently apply a hardcoded +8h offset (Australia/Perth, no DST). Godot 4 has no built-in tz database — options: bundle a tz library, self-roll DST handling, or punt to a plugin. MUST resolve before launch (any non-UTC+8-no-DST user sees install-date drift at midnight boundary).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PARKED — known, not blocking
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] Bump wrangler `compatibility_date` forward on next `npm update wrangler` (currently pinned 2026-01-01). Natural at Phase 5 prep.

[ ] Windows curl TLS handshake quirk. First request to new Worker URL can return `SEC_E_ILLEGAL_MESSAGE` for ~30min. Browsers work immediately. Workaround: browser-test first.

[ ] `wrangler dev` drift to remote mode. Workaround: `npx wrangler dev --local` (explicit flag). Adopted as convention.

[ ] Wrangler D1 token-refresh quirk. D1 import can fail with "Authentication error [code: 10000]" despite valid `wrangler whoami`. Fix: `npx wrangler logout` + `npx wrangler login`.

[ ] Coin economy balance pass (Phase 4 work). All numbers PLACEHOLDER. Specifically: per-task coin range (currently 900-1400, obfuscated by design), partner 1.5x multiplier behaviour, subscription bonus rate (+15-20% target), rest day cost per tier, pack cost curve (300→400→500→600 cap), streak multiplier interactions.
    Principles to respect: one week sub can't buy entire catalogue; free pair gets rest days ~4/month; subscription is "tip not class divide"; no per-sticker stacking bonuses.
    Belongs after 4.6 + basic picker. Dedicated sub-session with spreadsheet.

[ ] Subscription infrastructure plumbing — Phase 5 prep. RevenueCat SDK + webhook endpoint, `users.subscription_active` (server-set only, already in v1.0 schema), library access entitlement check, coin bonus application in claim, lapse notification flow, paywall UI.

[ ] Founders cutoff date locks (before Phase 5). Founders FLAG cutoff: "first TestFlight / internal Play track deployment." Hardcoded list ~20-30 people. Founders RATE expiry: "31 December year 2." Launch users eligible first 6-12 months.

[ ] Day-21 subscription disclosure copy. VALUE SUMMARY against actual play history: coins earned, packs owned vs catalogue, days played, longest streak. "Subscribe to access [partner]'s library and grind faster." Lands Phase 5 with paywall UI.

[ ] Pack drop content buffer — Phase 4 art prep. 3-4 packs queued before launch (half subscriber-only, half free-thank-you). First subscriber-only ~2 weeks post-launch, first free-thank-you ~4 weeks. Maintenance: 1-2 packs/fortnight. Honest skip-this-fortnight communication acceptable if buffer hits zero.

[ ] Content drops pipeline — schema + read endpoint + in-app popup rendering pre-launch; authoring tool post-launch. See `four_tasks_content_authoring_tool_design_notes.md`. Blocked on morning sequence beat collision resolution.

[ ] Theme slot catalogue gaps. Popup skin slot, meta mods definition. Theme design doc enumeration may be incomplete. Resolve before 4.14b.

[ ] Streak semantics under week mode. Does opted-out weekday break streak, count as success, or not count? Resolve before 4.9.

[ ] Same-person multi-device pairing. Distinct from group expansion. Architecture allows doppelgänger pair. Not currently a problem. Design needed if multi-device sync becomes v1.x.

[ ] Stamp messages real authoring. 5-10 per tier red/orange/yellow, 40+ green, 20 purple. Currently stubs in `stamp_messages.ts`. Content work, not v1.0-blocker.

[ ] Achievement framework — speculative. `four_tasks_achievements_brainstorm.md` (session 7). May never ship.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DEFERRED — v1.x or later
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] Push notifications.
[ ] Audio (silent v1, audio v1.1).
[ ] Partner reactions (`four-tasks/deferred/four_tasks_partner_reactions_design_notes.md`, deferred session 12).
[ ] Leaderboard (`four-tasks/deferred/four_tasks_leaderboard_design_notes.md`, session 8). Re-evaluate post-v1.0 with usage data.
[ ] Coin name generator + reroll (`four-tasks/deferred/four_tasks_coin_name_design_notes.md`, session 9). Schema columns reserved.
[ ] Per-user stat tracking. `four_tasks_tracking_design_notes.md` (session 8). Only lifetime_coins + longest_streak have committed UX.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPLETED LEDGER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

See devlog v2 SUMMARY OF SESSIONS 1-9 block + v1 archive for full history with hours, commit hashes, and design rationale.

Running project total at session 17 close: ~88.75h.