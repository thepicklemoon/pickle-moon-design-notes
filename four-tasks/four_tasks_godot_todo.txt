FOUR TASKS — Godot Todo
========================
Last edit: 2026-06-15 AWST (session 38, laptop)
Last updated: session 38 — 4.D2 WEEK MODE: SERVER + READ-SIDE CLIENT (authoring UI deferred to s39). ★ PILLAR CORRECTION: week mode is NO LONGER "personal/not shared" — the partner panel must be a FAITHFUL RECONSTRUCTION of the partner's own calendar (their default, overridden, and sealed-immutable days exactly as they render in their app). Morgan's call, hard reversal of the locked design doc's privacy framing; "no cross-user reads in v1.0" is DEAD (partner reactions remain scope-cut, but calendar mirroring is the pillar). SHIPPED + DEVICE-VERIFIED: schema.sql +active_mask on user_weekday_overrides (DBs reset, reset-not-migrate); PUT /users/:id/weekday_overrides/:weekday (9/9 wire-passed: round-trip, GET-carries, upsert, mask/label/weekday validation, partner-exclusion-now-INCLUSION); GET payload ships BOTH weekday_overrides (self) + partner_weekday_overrides (mirror). Client read path: State holds both override sets + resolve_labels_for_day(uid,row,date) is the SINGLE resolution authority (sealed snapshot → weekday override → standard four, per slot, keyed by uid so partner resolves against partner data); task_list delegates to it; CalendarMath.iso_weekday added; ★ SEAL RETROFIT DONE (snapshot now the RESOLVED four). DEVICE-VERIFIED: partner's overridden Wednesday ("HER WED", slot 3) renders on the partner panel exactly as authored. BUG FOUND+FIXED mid-session: poll handler (_on_poll_completed) didn't ingest overrides → they survived boot but died on next poll; now ingested on both paths. OWED: AUTHORING UI (4.D2 remainder — editor modal, weekday-header long-press, bold treatment, banner); week-mode DOC REWRITE (5 "private" passages now wrong); index.ts → PROD (still owed from s37, now carries 4.D2 too). Session 38 time: ~Xh ESTIMATE.

Forward-looking tile list organised by phase. Work topmost incomplete tile in current phase; drop down rather than jumping phases.

Completed-tile history with session attribution + time logged: devlog v3 SUMMARY block + v1/v2 archives.

Tags: [CODE] [ART] [CONFIG] [ADMIN] [DECIDE]
Status: [ ] not started, [~] in progress / design closed, [x] done
Convention: ticked tiles get `Time: ~Xh` or `~Xmin` on title line.
Tiers (for items outside phase structure): ACTIVE / PARKED / DEFERRED.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OPEN TESTS (post-ledger, s31)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Device ledger D1-D15 CLEARED s31; W18 + git case-rename CLEARED s33.
s34 UNVERIFIED LEDGER mostly CLEARED s36 (D-S1/D-S4/D-S5/D-S6/D-S7 PASS;
D-S2/D-S3/D-S8 PARKED — see below). Remaining:

  [ ] W10 (optional) Thursday cron — commands in wire_regression_pack.md,
      or let a real Thursday prove it on dev.
  [ ] Emoji glyphs missing on work-laptop Android export (🔥/🟡 streak bar
      + CoinFX flights invisible; ★ fine). Check Godot version/export
      parity vs home PC BEFORE any release build from the laptop.
  [ ] 4.26 ambient device cases (code GREEN D-N1-D-N4 s33; full pass spec
      in four_tasks_notifications_design_notes.md):
        D-N5 overnight morning nudge + no stale morning after open
        D-N6 permission denied → silence; re-grant → return
        D-N7 reboot with app closed → scheduled nudge survives
        swipe-kill variant — Morgan's phone AND Ayshe's (OEM battery
        managers differ; her D-N1 permission flow rides her first build)
  [ ] Strip the devkit feature tag from the daily-driver/prod build
      (devkit entry point + prod DB is a fat-finger risk; dev builds
      keep it). NOTE: store preset is devkit-clean as of s36 (verified
      via [Loader] devkit=false on the prod APK); this stays open as the
      standing per-build reminder.

  ── s34 LEDGER — PARKED REMAINDER (s36) ──
  [ ] D-S2 (4.24) Sealed-skip days dead + refuse taps, BOTH panels.
      Self panel PASS s36; PARTNER skip immutability UNTESTED — no
      partner skip seed exists. Verify when next altering the seed.
  [ ] D-S3 (4.24) Off-month nav — backward case + today-as-overflow
      edge UNEXERCISED s36: June 1 = Monday, no leading dead cells.
      Forward case (May → June trailing → navs + tray) PASS, carries
      the F1 fix proven live. Needs a month whose 1st isn't Monday —
      JULY 2026 (1st is Wed) gives leading cells. Verify then.
  [ ] D-S8 (4.10) Month stats popup long-press on a REAL sealed month
      — verify at first actual month seal. DevKit in-progress view PASS
      s36 (6/48, 13%, best run 0, coins '—', elapsed-day denominator)
      but that's dev convenience; the SHIPPING trigger is long-press on
      the tier-recoloured title, which only exists once a month seals.
      Sealed-only gate confirmed correct as design (title recolour IS
      the affordance). Confirm title-recolour + long-press read the
      SAME sealed-state flag when it first fires.

  [ ] index.ts → PROD deploy owed (s37 + s38): the 4.SL seal-write AND the
      4.D2 week-mode endpoint + partner-override mirror are on dev +
      device-verified, prod NOT yet deployed. npx wrangler deploy --env
      production from server/. Until then a real prod seal would not freeze
      labels AND prod has no week-mode endpoint. NOTE prod schema: prod still
      holds no real Ayshe pairing, so the active_mask add is still a reset
      (reset.sql + schema.sql --remote --env production) NOT a migration —
      the instant prod holds a real pairing this flips (see schema policy).

  [ ] [CODE] LOAD-FAILURE vs ACCOUNT-NOT-FOUND conflation (s38 finding,
      launch-hardening). The client stores user_id locally; on boot it
      load_user(saved_id). A 404 / user-not-found from the server is
      currently rendered identically to a network failure — "Couldn't reach
      the server. Tap to retry." — which is a LIE and an infinite trap: the
      row is gone, retry never recovers. Harmless from a dev DB reset (you
      re-seed), but in PROD a deleted account / dangling-id unpair edge /
      any legit "no such user for this stored id" path traps a real user
      forever. FIX: Loader/Identity must distinguish a 404-on-load (→ treat
      local identity as invalid: route to onboarding, or a "this account no
      longer exists — start fresh?" screen) from a genuine network error (→
      retry). Not on 4.D2 critical path; do before store launch. Invisible
      until someone hits it, then a support nightmare.

  [ ] Time-log corrections owed at close: s32 ~6h, s33 ~7h, s34 ~3h,
      s35 ~3h, s36 ~5h, s37 ~5h (all ESTIMATES — SIX pending).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
APPTRIOC FORK ARCHITECTURE (locked session 5)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

APPtrioc is a snapshot of Four Tasks at the end of tile 4.14a (pre-long-press picker), exported to HTML5, deployed to Cloudflare Pages, configured for unilateral pairing + atrioc-themed stickers + three-button onboarding. Separate Worker + D1.

Fork sits between 4.14a (basic picker, tap-to-toggle pool) and 4.14b (long-press context menu, theme manager). APPtrioc never gets post-fork tiles — they're the conversion pull.

Launch sequence: Four Tasks ships first (Phase 5), then APPtrioc (Phase 5b), then atrioc video + Reddit post drops. atrioc moment is force multiplier on shipped product, not launch driver. See `four_tasks_marketing_notes.md`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 0 — Pre-flight (CLOSED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] 0.1 [CONFIG] Godot project created. Session 1. ~1h.
[x] 0.2 [CONFIG] Project settings (1080×2400, Nearest, portrait). Session 1. ~1h.
[x] 0.3 [DECIDE] Fonts imported (Departure Mono + JetBrains Mono); body font deferred to Phase 2/3. Session 1. ~1h.
[x] 0.4 [CONFIG] Devkit skeleton (ungated until tile 5.2). Session 1. ~30min.
[x] 0.5 [ADMIN] ABN + accountant. Session 1. ~1.5h.
[x] 0.6 [ADMIN] myGovID + RAM. Session 1. ~25min.
[x] 0.7 [ADMIN] Business name THE PICKLE MOON registered ($104, 3-year). Session 1. ~20min.
[x] 0.8 [ADMIN] Mac access deferred to Phase 5. Session 1. ~10min.
[x] 0.9 [DECIDE] Monetisation locked. See `four_tasks_monetisation_position.md`. Session 1. ~1.5h.
[x] 0.10 [ADMIN] Privacy policy v7 in `pickle-moon-public`. Session 1. ~30min.
[x] 0.11 [DECIDE/CONFIG] Ledger live (`pickle-moon-ledger`). Session 1+2. ~1.25h.
[x] 0.12 [CONFIG] Dev environment. Session 2. ~15min.

GATE: Closed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1 — Backend + foundations (CLOSED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] 1.0 [CONFIG] Cloudflare account. Session 1. ~10min.
[x] 1.0a [DECIDE/ADMIN] Domain `thepicklemoon.com` registered. Session 1. ~30min.
[x] 1.0b [CONFIG] Toolchain (Node v24.15.0 + Wrangler 4.90.0 + OAuth). Session 1. ~25min.
[x] 1.1 [CODE] Worker scaffold + D1 schema. Session 2. ~3h.
[x] 1.2 [CODE] Worker endpoints v1 (rewritten to user_id model session 12). Sessions 3+12. ~3h + rewrite.
[x] 1.3 [CODE] Worker defensive write rules — ten write endpoints, v1.0 schema (7 tables, 36 cols), all curl-verified + deployed. Session 12. ~5h.
[x] 1.4 [CODE] Lazy seal-on-open. ABSORBED into 1.3 claim transaction. Session 12.
[x] 1.5 [CODE] Godot Backend autoload. Time: ~30min.
[x] 1.6 [CODE] Godot State autoload. Time: ~30min.
[x] 1.7 [CODE] Godot Identity autoload. Time: ~30min.

GATE: Closed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2 — Visible app skeleton (CLOSED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] 2.1 [CODE] Loader scene. Time: ~1h.
[x] 2.2 [CODE] App scene with two-panel swipe. Time: ~1h.
[x] 2.3 [CODE] Calendar grid — 9-state cell model, bounded month nav. Sessions 13-15. ~5h.
[x] 2.4 [CODE] Task list. Session 15. ~2h.
[x] 2.5 [CODE] Tasks tray. Session 15. ~1.5h.
[x] 2.6 [CODE] Streak bar — self panel. Session 16. (2.6+2.7 ~2h combined.)
[x] 2.7 [CODE] Bonus pill — partner panel. Session 16 (+today_iso fix s23).
[x] 2.8 [CODE] Partner panel polling (30s/paused/10s-deferred + selective merge). Session 16. ~1.5h.
[x] 2.9 [CODE] DevKit — scenario menu, dev worker + D1, flag-gated. Session 18. ~1h.
[x] 2.A [CODE] Panel heading + project-wide font + Prefs.gd. Session 18. ~2h.

GATE: Closed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3 — Onboarding + identity flow (CLOSED session 25)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Reference: `four_tasks_onboarding_design_notes.md`. Solo-as-default, 8-screen
flow, pairing post-onboarding. VALIDATION (every name/username/sticker input):
no `|`, non-empty; UX caps username ~20 (server 40), sticker ~short (server
50), task labels ~30 (server 49). Server caps ENFORCED as of session 29
(value_too_long). NOTE (s36): the createUser SERVER path itself was never
exercised against a real DB until s36 — devkit seeds bypass it. Now proven
(curl smoke + first prod boot). Pairing / join_by_values / recovery_flow
remain device-only-tested, not first-contact-proven against empty prod.

[x] 3.1 [CODE] Onboarding state machine + entry routing. Session 24.
[x] 3.2 [CODE] Welcome + first-time/recover fork. Session 24.
[x] 3.3 [CODE] Name + immutable confirm. Session 24.
[x] 3.4 [CODE] Username. Session 24.
[x] 3.5 [CODE] Starting avatar (layout only; theme theatre = Phase 4 seam). Session 24.
[x] 3.6 [CODE] MOTD (6) — real as of 4.4 (session 26).
[x] 3.7 [CODE] Partner's calendar (7) — placeholder; real surface is 3.10A in App.
[x] 3.8 [CODE] Coins/picker, closing (8) — placeholder (economy-fenced).
[x] 3.9 [CODE] Four-tasks entry. Session 24.
[x] 3.10A [CODE] Pairing — "Add my partner" join flow + partner_empty_state. Session 24.
[x] 3.10B [CODE] Recovery flow — two-tier, prod-verified both directions. Session 25. Time: ~4h.

GATE: CLOSED (session 25).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4 — Skeleton (ALL CODE, build now)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Re-split session 25. Phase 4 = EVERY code tile — the daily loop made fully
usable. Art is NOT a dependency: stub/placeholder art is valid everywhere;
real painting is Phase 4b. Tiles that READ art build against placeholder PNGs.

Fork sits between 4.14a (pre-fork, APPtrioc inherits) and 4.14b (post-fork,
Four Tasks exclusive).

FORECAST (2026-06-10 review): remaining Phase-4 code ≈ 90-105h at current
velocity ≈ ~3 roster breaks to code-complete. Critical-path design
conversation: THEME PREP (gates 4.14b; schedule first thing next break, doc
before code). Realistic store launch: Sept-Oct 2026.

━━━ PRE-FORK CODE TILES (APPtrioc inherits) ━━━

[x] 4.1 [CODE] Glitch typewriter utility. Session 25. ~1h.
[ ] 4.2 [CODE] Wire glitch-word reveals into onboarding popups. 24 marked words from web build (NEEDED from Morgan). (~2-3h)
[x] 4.3 [CODE] CoinFX utility. Session 27. (Stub coin art; real sprite 4.A — swap _make_coin only.)
[x] 4.4 [CODE] MOTD spinner. Sessions 25-26. ~6h. TUNING deferred (column widths, font, selector band, emoji reel TextureRect column). NOTE (s36): spinner shows WORDS where sticker art expected on prod build — same sticker-load gap as onboarding companions; see ACTIVE sticker-render bug.
[~] 4.5 [CODE] MOTD reroll cost — SERVER HALF DONE (s29): POST /users/:id/motd/reroll (relative debit, 409 insufficient_coins, returns user + fresh roll); REROLL_COST=100 placeholder shipped via economy payload; fee-is-vent-not-protection decision recorded at the constant (free /motd/roll remains the first-spin path). CLIENT HALF remains: picker lock button + confirm popup wiring (ceremony-adjacent — with Morgan). MUST sealed-gate the edit surface (sealed-beats-clock, s31) AND serve as the recovery surface for a mid-ceremony kill (today's MOTD pick is lost if beats die before 9 — s31 finding). (~2h left)
[x] 4.6 [CODE] Morning sequence — coordinator. Sessions 26-28. ~11h + economy. OWED: in-context tuning (wobble dir, coin size, picker card width/entrance, MOTD font, beat-5a hold); cross-month verified-on-real-1st only. Interruption = freeze/resume (doc synced s32).
[x] 4.7 [CODE] Stamp slap — folded into 4.6 beat 7. Session 26. Placeholder art; real stamps 4.C. Messages AUTHORED + live s36 (see stamp_messages.ts; pools locked).
[~] 4.8 [CODE] Rest day system — MECHANISM DONE (s28) + session-29 hardening (free-rest hole closed, per-op rollback, server-shipped cost, anim-leak fix). Time: ~2h. Device test = S29 ledger D7/D8. DEFERRED: rest-day task-label content swap (~15-30 phrases; felt cheesy).
[~] 4.10 [CODE] Month colour + stats popup — RESHAPED s35 (authority NEW four_tasks_month_stats_design_notes.md): month STAMP CUT (visual downgrade vs eternalised themed month; PERFECT MONTH survives as a single client flourish at 100% — pools-doc month-stamp question CLOSED, no pool). Popup half BUILT s35, DevKit in-progress view DEVICE-VERIFIED s36 (D-S8 dev-side: %, X/Y, best run, coins '—', elapsed denominator all correct): month_stats_popup.gd, code-built overlay (bug_catcher pattern), bounded in-popup month nav, static month_stats()/tier_for_percent() helpers the header wiring imports. coins STUBBED '—' (no per-day coins server-side; per-day column = mechanism A at next index.ts unfreeze, doc records the lifetime-watermark alternative B). REMAINS: (a) header wiring — month-name tier tint (completed months only, band consts in the popup) + long-press relay, few lines in calendar_grid AFTER D-S2/D-S3 verify [confirm tint + long-press read the SAME sealed flag]; (b) coins column server-side post per-day unfreeze; (c) COMPLETED-MONTH CHROME — DESIGNED s35 (theme doc v2 authority), builds inside 4.14b; (d) D-S8 SHIPPING-trigger verify at first real month seal. Month-close disclosure copy TBD (roster content list). Time: ~1.5h popup (ESTIMATE).
[ ] 4.11 [CODE] Help menu — two-tier, six subjects, [g]glitch[/g] markup (consumes 4.1). Copy = roster-week content list. (~4-6h)
[~] 4.12 [CODE] Bug catcher — MECHANISM DONE (s29): bug_catcher.gd (code-built, self-contained compose→sending→thanks/failed, 2000-cap counter, ONE_SHOT teardown on every exit), triggered from DevKit → Dialogs. Test = D11. REMAINS: real-UI trigger (one line, lands with 4.11's help menu). Confetti garnish DEFERRED with 4.9 (s35) — no one-off particles; rides the shared system if 4.9 revives. (~5min left, inside 4.11)
[ ] 4.14a [CODE] Basic sticker picker — tap-to-toggle pool membership ONLY. Floor of 1. Themed-sticker marker rendered (APPtrioc conversion pull). Build against 4 existing stickers + stub PNGs; sparse is valid. (~4-6h)
[x] 4.14c [CODE] Future cells non-tappable. day_cell.is_interactive() rejects taps + long-presses on DEAD_FUTURE. Device-verified session 31. ~0h.
[x] 4.SL [CODE] Sealed label snapshot + standard-four editor — s37. days.sealed_labels (JSON[4], the resolved four frozen at seal; '[]' → live fallback). Server (index.ts): sealSpan writes 3 seal sites + parseDayRow serialises + seedDefaultSpread backfills seeded-sealed days (they bypass sealSpan — every seeded sealed day was '[]' → live → looked broken; that was the debug arc). Client: task_list render rule (_resolve_labels_for_day) + State passthrough/ceremony snapshot. NEW tasks_edit_dialog.gd (all-code modal) reached via hamburger_button.gd long-press; app_menu.gd DELETED (long-press opens editor directly). Both DBs reset from schema.sql (new reset.sql; reset-not-migrate). DEVICE-VERIFIED: sealed days held, today changed; the claim-sealed day returned a populated snapshot (proves sealSpan, not just backfill). OWED: index.ts → PROD. Authority: four_tasks_task_label_editing_design_notes.md. Time: ~5h EST.
[ ] 4.FT [CODE] Fourth-tick seal — completing the active four seals the day immediately with the FULL seal behaviour (stamp / theme snapshot / label snapshot / payout / sealed_at). Currently tasks stay mutable for testing. RULE: route through the SAME seal routine as sealSpan — trigger inside the day-write endpoint when a write fills the four; never a hand-rolled "seal today" that sets sealed_at and forgets the snapshots (the exact 4.SL bug class). Client mirrors via _apply_seal_to_day. Forks to settle at build: (a) idempotency vs the 4.25 walkback — a self-sealed today must be skipped next morning, no double stamp/payout; (b) commitment/undo — the fourth tick locks the day instantly, decide confirm/grace window vs intended feel. (~3-5h)
[~] 4.D2 [CODE] Week mode — STAYS v1.0 (2026-06-10). Per-weekday per-slot
    task-label overrides that propagate to all matching not-yet-sealed
    weekdays. ★ PILLAR CORRECTION (s38, Morgan): NO LONGER "personal/not
    shared" — the partner panel is a FAITHFUL RECONSTRUCTION of the partner's
    own calendar (default + overridden + sealed-immutable, exactly as it
    renders in their app). "No cross-user reads in v1.0" KILLED (partner
    reactions stay scope-cut; calendar mirroring is the pillar). Design doc's
    5 privacy passages are now WRONG — REWRITE OWED.
    SERVER DONE + DEVICE-VERIFIED (s38, dev): schema.sql +active_mask (DBs
    reset); PUT /users/:id/weekday_overrides/:weekday (self-write, whole-row
    idempotent upsert, 9/9 wire-passed); GET ships weekday_overrides +
    partner_weekday_overrides (fetchWeekdayOverrides generic by uid, called
    for both sides; solo branch sends []). INVARIANT: every slot always
    NON-EMPTY (validateTaskLabels rejects empties) — paused slot keeps text
    w/ mask bit cleared, never-authored carries the standard-four placeholder;
    mask alone decides active/inactive (stronger than the doc's "normalise
    empty active → inactive": empties can't enter). Backend.write_weekday_
    override + signals wired.
    READ-SIDE CLIENT DONE + DEVICE-VERIFIED (s38): State holds self_/partner_
    weekday_overrides (ingested on BOTH _on_user_loaded AND _on_poll_completed
    — the poll path was the s38 bug: overrides survived boot, died on poll);
    State.resolve_labels_for_day(uid,row,date) = THE single resolution
    authority (sealed snapshot → override bit-set+non-empty → standard four,
    per slot, keyed by uid → partner resolves against partner data = the
    mirror); task_list._resolve_labels_for_day delegates to it (task_list
    shrank, its own _resolve_task_labels/_extract_sealed_labels DELETED);
    CalendarMath.iso_weekday(date)→1-7 added. ★ SEAL RETROFIT DONE:
    _apply_seal_to_day snapshots the RESOLVED four (was the flat standard
    four; _self_labels_snapshot DELETED, superseded). VERIFIED: partner's
    overridden Wednesday slot 3 ("HER WED") renders on the partner panel.
    AUTHORING UI REMAINS (s39): the override editor modal (second modal,
    cloned from tasks_edit_dialog scaffold — checkbox column + pause
    semantics + the new endpoint; NOT a mode-flag on the standard-four
    dialog); weekday-HEADER long-press → opens it scoped to that weekday (the
    MON-SUN header row already exists in calendar_grid.tscn as static labels,
    NOT a strip to build — wire a long-press onto the labels); active-override
    weekday header BOLDED (treatment tunable later); hamburger-editor banner
    gated on today's weekday having ≥1 active override. Authority:
    four_tasks_week_mode_design_notes.md (REWRITE the privacy sections first).
    Primer: four_tasks_week_mode_primer.md. Time so far: ~Xh EST.
    Interaction-model fork RESOLVED: Model B — single POPUP off the weekday header, four task slots with per-slot override controls (Morgan 2026-06-10; staggered-disclosure doc synced s32: day-2 long-press relays into the popup, one-time intro line replaces the tutorial flow). Doc sync + popup detail DONE s34 (week-mode doc fully rewritten to Model B; Morgan decisions 1-10 locked: per-slot independent toggles + text, empty-on-blur revert, ghost placeholders, tick-greying w/ all-ticked-unlocks rule, one whole-row save, PUT /users/:id/weekday_overrides/:weekday, week_mode_weekdays column retired-dead). REMAINS: the authoring UI (see tile head). (~6-9h left of original ~8-12h est).
[~] 4.23 [CODE] App local-date-change watcher — BUILT s34, DEVICE-VERIFIED s36 (D-S4 PASS): _last_seen_date marker + _check_date_rollover() first on every FOCUS_IN: date moved EITHER direction → both calendars jump_to_today(); open trays on now-future dates close; payout ceremony is one-shot, consumed at accounting (2nd mock-day re-rolls MOTD only, correct). Skipped mid-ceremony WITHOUT consuming the marker. Time: ~30min.
[~] 4.24 [CODE] Dead-cell treatment + off-month nav — DECIDED + BUILT s34, DEVICE-PARTIAL s36. Authority: four_tasks_calendar_cells_design_notes.md. Sealed 0-tick days → dead-inert, uninteractable, no tray; pre-install identical; off-month flattened, tap = grid navigates home then delegates as in-month tap (direction-agnostic; navigation-only when grey/pre-install at home); grey stamp tier LATENT. Build: day_cell.gd (SEALED_GREY, OFF_MONTH, flattened dead visual, F1 DeadOverlay MOUSE_FILTER_IGNORE fix s35) + calendar_grid.gd (sealed branch, nav-delegate). DEVICE s36: grey skips dead self-panel PASS; FORWARD off-month tap (trailing → navs + tray) PASS, F1 fix proven live. PARKED: partner-panel grey immutability (D-S2, no seed), BACKWARD off-month + today-overflow (D-S3, needs July). Time: ~1h.
[~] 4.25 [CODE] Walkback span-sealing — SERVER HALF DONE s33 (wire-passed) + CLIENT DEVICE-VERIFIED s36 (D-S6 PASS, tile CLOSED): grey rescue dialog fires correct (grey tier, streak-at-stake 3, cost 50000); broke balance DISARMS the SAVE button (affordability gate working — only arms when affordable); LET IT GO seals the missed day + runs payout (decline path, streak→0, no charge); funded accept → rest-day beat (separately seen). Server: buildSpan + evaluateSpanRescue + sealSpan, per-day streak walk, shared sealed_at, W13r/W14r/W19/W20/W11/W12 wire-passed s33; wind-down null-target greys paint via refresh_after_morning (D-S5 PASS). DONE.
[~] 4.26 [CODE] Notifications — BUILT s33, DEVICE-PARTIAL. Design authority: four_tasks_notifications_design_notes.md (times 6:30am/8pm fixed v1.0; lowercase copy; boot permission; structural suppression — day-0 morning never scheduled, evening skips 4/4 + rest day; lapse decay daily*7 then gap+1/nudge then weekly, horizon 56; mock clock = silent). Plugin godot-mobile-plugins/godot-notification-scheduler; gradle build now ON (laptop). Notifications.gd autoload, zero existing-file edits. D-N1-D-N4 GREEN; tile closes when the ambient cases (open tests block) go green on both phones. Time: ~3h (ESTIMATE).
[~] 4.27 [CODE] Safe-area top inset — BUILT s34, DEVICE-VERIFIED s36 (D-S1 PASS, self-phone): Layout.safe_top_inset() (DisplayServer safe-area top edge, physical→design conversion, mobile-gated 0 elsewhere); App._apply_safe_area_inset() (inset onto panel VBox, off each tray's runtime minimum). Boot print confirmed "[App] safe_top_inset=172", title clear of bar, tray fits. SOUTH-PHONE REPEAT still owed (CAVEAT: barred-but-cutoutless devices report 0 — theoretical for the pool). Loader/onboarding eyeballed clear. Time: ~30min.

━━━ FORK POINT ━━━ APPtrioc launches from snapshot after 4.14a. See PHASE 5b. ━━━

━━━ POST-FORK CODE TILES (Four Tasks exclusive) ━━━

[ ] 4.14b [CODE] Sticker picker context menu + theme manager. Long-press (500ms + vibration). Unlocked menu: per-slot icons, instant application + APPLY ALL. Locked menu: PREVIEW (~30s revert). Rotation fires on active_leader change ONLY.
    Design authority: theme doc v2 (THEME PREP resolved s32 — colour system, 7-slot catalogue, pre-bake retint, preview/marker rules; +s35 COMPLETED-MONTH CHROME — completed months source screen-level slots from the last sealed accounted day's day_theme_state, one selection rule inside the renderer; build-time check: days payload ships accounted_for, else one-line payload add at unfreeze). Build machinery against placeholder art. Owed at this tile per doc: icon art, marker style, 9-slice centre-mode convention, partner-render asset endpoint shape. (~10-15h)
[ ] 4.15 [CODE] Username + icon edit. Settings → triggers rotation. "Changes may take a minute" popup. (~3-4h)
[~] 4.20 [CODE] Palette derivation devtool — BUILT s35, DEVICE-VERIFIED s36 (D-S7 PASS, tile CLOSED): tool opens DevKit→Tools, scans res://assets/icons recursively (RES_SCAN_DIR fixed s36 — was assets/stickers), renders nearest preview + role swatches w/ hex/count/%, neutral + alpha audits, CLOSE adjacent-rank advisory + AA-smell overflow surface all fired correctly on placeholder stickers. Inbox load (user://palette_inbox) fixed s36 — Image.load_from_file wrapped in ProjectSettings.globalize_path (Android user:// → OS path). palette_derive.gd NEW (class_name PaletteDerive): SHARED derivation 4.14b's renderer MUST consume — int-exact exclusion (a8<255 OR r8==g8==b8), derive() w/ role ranking + neutral audit + alpha + compressed-import flag, role_color() accent→tertiary fallback. NOTE: the two designated neutral greys must be present in PaletteDerive's exclusion set so documented greys drop to neutral (per pools/theme doc — confirm once). devkit_palette_tool.gd NEW + devkit_menu.gd Tools row. Time: ~2h.
[ ] 4.21 [CODE] Un-pair flow — client UI (server endpoints done 1.3; initiator-notice fix s29). Initiator: long-press partner header → menu → Un-pair → confirm → change-username follow-up. Recipient: dismissable banner (one-shot, server-tracked). Reference: pair-key doc §17. (~3-4h)
[~] 4.22 [CODE] Content-drops delivery pipeline — SERVER RECEIVE-SIDE DONE (s29): schema (content_drops / content_drop_assets / content_drop_deliveries), GET /users/:id/drops (live + forward-only + undelivered; release-day local-time gating is CLIENT-side per client-owns-the-clock; subscriber_only returned not filtered — sub-pack news is conversion pull), POST delivered ack (explicit, idempotent, never implicit on read), Thursday-release cron (wrangler [triggers] + scheduled handler). Tests = W9/W10. CLIENT HALF remains: presentation queue + drop popup (ceremony-adjacent — with Morgan). Publishing = manual SQL at launch; authoring tool post-launch. (~3h left)

MOVED OUT OF PHASE 4: 4.13/4.13a leaderboard (KILLED for v1.0, 2026-06-10 —
see DEFERRED; eternal-stats optionality preserved); 4.9 streak popup
(DEFERRED v1.x, s35 — see DEFERRED); 4.16 partner reactions,
4.17/4.18 coin names, 4.19 TutorialCoordinator (all v1.x, see DEFERRED).

GATE: The daily loop is fully usable on stub art. Tick → seal → ceremony →
payout → stamp. MOTD spins, reroll costs. Rest days work. Picker toggles the
pool, theme manager applies. Week mode works. Un-pair works. Content drops
render. Everything functions; nothing is painted yet.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4b — Art & feel (PAINTING ONLY, at the easel)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] 4.A [ART] Coin sprite.
[ ] 4.B [ART] Frog mascot.
[ ] 4.C [ART] Stamp set. Five tiers: red/orange/yellow/green/purple (grey LATENT s34 — never surfaced, paint nothing).
[ ] 4.D [ART] Sticker catalogue. Folder per sticker, `<id>_<role>.png`. Palette from sticker.png pixel-frequency (≤4 non-neutral + free neutral ramp — doc v2). 32×32 RGBA. Maximal pack = sticker + background 360×800 + three 48×48 9-slices + 1-4 border corners 96×96. Launch set + 3-4 pack buffer (see PARKED pack-drop item). Reference: theme doc v2. NOTE (s36): loaders expect <folder>/<folder>_sticker.png (palette tool confirmed black_cat_sticker.png etc. on disk); see ACTIVE sticker-render bug — picker/spinner aren't resolving these at runtime in BOTH builds, root cause TBD (filename convention vs loader path template).
[ ] 4.E [ART] UI chrome (buttons, popups, dividers, util bar).
[ ] 4.F [ART] Onboarding example calendar screenshots (real build, post-theming).

GATE: App looks like Four Tasks. Skeleton + art = shippable build.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5 — Ship Four Tasks to stores
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

>>> BUY A FUCKIN MAC <<<
(Deferred from 0.8. Options: M4/M5 mini, MacInCloud, borrow. Needed before 5.5.)

[ ] 5.0 [DECIDE] Android-first option. Considered 2026-06-10, NOT taken (store-spend at launch matters; alienating iOS adopters feels wrong) but kept as a conscious lever if Phase 5 slips: Google-only launch halves Phase 5, APPtrioc (HTML5) doesn't need iOS, iOS fast-follows on revenue. Revisit only if the Mac/review path stalls.
[ ] 5.10 [ADMIN] Google Play Console setup. ★ PULLED FORWARD (2026-06-10): DO NEXT ROSTER BREAK. Identity verification has days-to-weeks lead time, and IAP products must exist in the console before RevenueCat plumbing is testable. Costs an evening now; costs a break if discovered late.
[ ] 5.1 [CONFIG] Production Worker deployment. Real domain (`api.fourtasks.thepicklemoon.com`). Builds → production; dev/test → staging. NOTE (s36): prod Worker `four-tasks-api` IS live + serving (workers.dev URL); `--env production` proven; devkit gating fail-closed (POST /devkit → 404). Custom-domain mapping is what remains here.
[ ] 5.2 [CONFIG] DevKit excluded from release exports — audit the custom feature is absent from store presets; confirm both devkit buttons vanish in a release build. NOTE (s36): store preset is devkit-clean (verified [Loader] devkit=false on prod APK). Keep the formal audit at Phase 5.
[ ] 5.3 [CODE] Production logging + error reporting.
[ ] 5.s [CODE] Subscription infrastructure plumbing — RevenueCat SDK + webhook endpoint SETTING users.subscription_active (server-set only; claim-side economy already reads it, s28), entitlement check, lapse flow, paywall UI, day-21 value-summary disclosure copy. Depends on 5.10 console products existing. (~10-15h)
[ ] 5.4 [CONFIG] Android build — release signing, keystore, signed .aab, verify on Pixel. (Debug toolchain done s23.)
[ ] 5.5 [CONFIG] iOS build — Xcode export, signing, archive, TestFlight, device install. (Mac gate.)
[ ] 5.6 [ART] App icon master 512×512.
[ ] 5.7 [ART] Store screenshots. 4-6 portraits per store.
[ ] 5.8 [ART] Feature graphic Google Play. 1024×500.
[ ] 5.9 [ADMIN] Privacy policy finalised + hosted. Align with honest threat model (pair-key §9 — incl. the s29 partner-user_id note, §9.1a).
[~] 5.9a [ADMIN] Terms of Service — DRAFTED (s29, `terms_of_service_draft.md`): plain-language, ACL carve-out, store-billing refund posture, pair-visibility honesty, 13+ age line. REMAINS: Morgan review (+ consider professional read before paid launch) + host in pickle-moon-public.
[ ] 5.11 [ADMIN] Apple App Store Connect setup.
[ ] 5.12 [ADMIN] Google review submission.
[ ] 5.13 [ADMIN] Apple review submission. Reviewer notes: "use pair name testa / partner testb to bypass onboarding."
[ ] 5.14 [ADMIN] Address review feedback.
[ ] 5.15 [ADMIN] Quiet launch via subculture seeding (Reddit/Discord). atrioc video held until 5b completes.

GATE: Two store listings, two live apps. Quiet launch. APPtrioc not yet live.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5b — APPtrioc fork + ship
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] 5b.0 [DECIDE] APPtrioc naming sanity check. Clean → ship; hesitant → DM permission first.
[ ] 5b.1 [CODE] Snapshot at fork point. Branch `apptrioc-fork-base` after 4.14a closes.
[ ] 5b.2 [CONFIG] Create `pickle-moon-apptrioc` repo. Private to start.
[ ] 5b.3 [CODE] APPtrioc backend — separate Worker + D1, unilateral, ~5-6 endpoints. Zero shared code. (Design pass first — see ACTIVE.)
[ ] 5b.4 [ART] APPtrioc sticker set + theme art. 5-8 hand-painted.
[ ] 5b.5 [CODE] APPtrioc skin + config. Three-button onboarding with password gates. Default partner = atrioc. Tooltip: "want themes? Four Tasks has more."
[ ] 5b.6 [CODE] Atrioc chain-import sweetener.
[ ] 5b.7 [CONFIG] Godot HTML5 export pipeline. PWA manifest + service worker.
[ ] 5b.8 [CONFIG] Deploy to Cloudflare Pages. `apptrioc.thepicklemoon.com`.
[ ] 5b.9 [ADMIN] Leak passwords 1-2 weeks pre-video.
[ ] 5b.10 [ADMIN] Record launch video. 10min, self-shot. Hard 20h edit budget.
[ ] 5b.11 [ADMIN] Launch — video + Reddit + APPtrioc live, same day.

GATE: Both products in market.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ACTIVE — ad-hoc items found during the port
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[x] Session 29 full-stack bug sweep + 20 fixes. Verified s31 (ledger cleared).

[x] ★ THEME PREP design conversation — RESOLVED session 32 (mobile grilling, screenshots + sketch). Theme doc v2 is the authority: colour system (4 weighted roles + role-accent fallback, neutral ramp #000/#5A5A5A/#B4B4B4/#FFF, R==G==B exclusion), v1.0 7-slot catalogue (leader/palette/background/calendar card/tray card/popup skin/border — cells SACRED, treatments deferred v1.x), pre-bake retint LOCKED, meta-mod transform stack (v1.x feature, v1.0 hook), dimension table from Layout.gd, R2 runtime asset delivery incl. partner-render rule, 30s preview. [TUNE AT FIRST ART] items live in the doc's open list. UNBLOCKS 4.14b/4.20/4.D. Time: in s32 total.

[x] ★ Morning-sequence additions design conversation — RESOLVED + BUILT session 30, device-verified s31. Time: ~design 1h + build 4h.
    (a) STREAK RESCUE — locked + built + verified (W11-W17, D12-D14). Authority: economy doc (Streak rescue) + morning-seq doc (Streak rescue gate). AMENDED s32 for span-sealing (4.25).
    (b) SEAL-DAY HERO BEAT — locked + built + verified (D15). Authority: four_tasks_hero_beat_design_notes.md. Sticker art placeholder until 4.D.

[ ] ★ SCHEMA MIGRATIONS — adopt before Phase 5 (s36, hard lesson). CREATE TABLE IF NOT EXISTS will NOT restructure an existing table — prod `days` survived as a stale pre-accounted_for version and broke createUser until DROP+reapply (safe only because prod was empty). Dev `days` ALSO drifted (hand-run ALTER, column appended at table end vs file's mid-table position) — functionally equal but the schema-of-record was unreliable. ACTION: (a) declare schema.sql canonical; (b) one-time reconcile dev↔file↔prod, confirm EVERY table matches the file in content (the hand-ALTER risk isn't just `days`); (c) introduce numbered migrations/ + a schema_version marker so future column adds apply to all environments via explicit ALTER, run separately from the CREATE-IF-NOT-EXISTS baseline. CRITICAL: drop+reapply dies the instant prod has a real (paying) user — this must be in place before Phase 5.

[ ] user_not_found should route to ONBOARDING, not the reach-server error screen (s36). State.gd:548 _on_user_load_failed / Backend.gd:493 _request collapse all load failures into "Couldn't reach the server." Latent — only bites a device holding a stale user_id against an empty/reset DB (genuinely-fresh install has no id → onboards fine), but real cases exist: server-side account deletion/unpair, any prod reset. Fix = branch: network/timeout → retry screen; clean user_not_found from a reachable server → onboarding. (~few lines)

[ ] STICKERS not rendering at runtime — MOTD picker + onboarding companions, BOTH dev AND prod builds, non-fatal (falls back to text/warning). onboarding_companions.gd:185 _load_sticker_texture; water_bottle the live example ("sticker art missing at res://assets/icons/water_bottle/water_bottle_sticker.png"). Root cause TBD: constructed path template vs actual filenames (loaders build an exact path string; palette tool lists them fine because it SCANS — different mechanism). Check the path template against disk + confirm water_bottle's file follows the <name>/<name>_sticker.png convention. NOT export-stripping (fails in both builds).

[ ] FIRST-RUN SURFACE broadly unverified (s36). Devkit seeds bypass createUser / first-pair / day-0. createUser PROVEN s36 (curl smoke + first prod boot); pairing, join_by_values, recovery_flow still only device-tested, not first-contact-proven against an empty prod DB. Expect more first-contact bugs as Morgan + Ayshe dogfood. Watch these paths specifically during pairing.

[ ] PARKED (s35 sweep, F2): days payload window (Jan 1 of last year) contradicts the calendar/stats nav floor (install month) — months older than the window render UNMARKED locally (tiers + sealed greys lost; 4.24's symptom resurfacing at the window edge), and each Jan 1 the poll merge mass-drops a year of local rows mid-session. Year-2-user class, nothing burns at launch. Morgan direction: likely resolved by a year-close export/ceremony concept rather than widening the payload — design when it matters.

[ ] Username display normalisation vs recovery footgun. panel_heading lowercases at render; recovery matches STORED case → user types what they saw, gets user_not_found. Resolve: (a) lowercase server normalisation (heavy: re-derives hashes), or (b) recovery-screen copy "enter exactly as you first typed it, capitals matter" (~30min). Lean (b). Pair-key doc §12.

[ ] Max display length for headings. Ellipsis probably. Pre-Phase-5 polish.

[ ] On-touch jitter tuning pass. DRAG_DEADZONE / TAP_DRAG_THRESHOLD on real fingers.

[x] DevKit mockable clock — BUILT (s29), device-verified s31 (D10).

[x] DevKit disappointment-day scenario — superseded by s30 morning_grey scenario (device-verified s31).

[ ] Roster-week content list — mobile-writable work for on-roster weeks: stamp-message additions OPTIONAL (pools LOCKED + LIVE dev/prod s36 at purple 18 / green 25 / yellow 4 / orange 3 / red 3 — append-over-time, one-deploy; NO longer bracket-stubs in prod), help-menu copy (six subjects), month-close disclosure copy (one-shot, disclosure doc s35), rest-day task-label phrases (~15-30), sticker/stamp concept lists, promo-codes doc draft.

[~] Doc sync pass — s29 shipped five docs; s32 shipped five more (economy, morning-seq — incl. the s30 rescue-gate section that never reached the repo, system map full refresh incl. the §6-7 inventory, timezone incl. boot-only clarification + rotation/MOTD wording, staggered-disclosure) + wire_regression_pack.md. REMAINING pile:
    - Partner reactions doc → `four-tasks/deferred/`.
    - `pickle_moon_repo_index.md`: reactions → deferred; promo codes doc ADDED (s29); ToS draft ADDED; wire_regression_pack.md ADDED + session wire-test files REMOVED (s32); theme doc → v2 (s32); schema migration-files-deleted note; write_rules → Background/Superseded.
    - Project system prompt: drop "first feature with field-level write rules".
    - Terminology: `diary` → `motd`, `emoji` → `active_leader` across remaining docs.
    - onboarding: name confirm displays typed value EXACTLY.
    - content_authoring doc: note receive-side shipped (4.22 s29) + beat-collision blocker RESOLVED (s26 presentation queue).

[x] Promo codes design doc — WRITTEN (s29): `four_tasks_promo_codes_design_notes.md`. One-code-per-user-ever; permanent founders rate; attempt logging incl. failures; codes never grant coins; founders FLAG vs RATE distinction. Redemption endpoint + client entry remain Phase-5 commercial work.

[ ] identity.cfg backup hook (Keychain/Keystore/cloud). NOT v1.0-blocking; recovery flow covers device replacement. Focused conversation Phase 5-ish.

[ ] Update devlog AI Reading Guide for repo-fetching workflow (raw URLs via repo index, curl over web_fetch for fresh pushes).

[ ] Log Cloudflare domain receipt in ledger. Paid 11 May 2026, IN-64839053, USD $10.46 (~A$16-17). Renewal 10 May 2027.

[~] Marketing notes — running document.

[ ] APPtrioc backend prep — design pass before 5b.3 (schema, bilateral-inside-unilateral special case, password gates, endpoint surface + write rules, rate limiting). Own doc in `pickle-moon-apptrioc` when repo exists.

[ ] APPtrioc-to-Four-Tasks keepsake export. Phase 5b or post-launch.

[ ] Year-end keepsake export. `GET /pair/:key/year/:year`. First fires Jan 1 2027; launch by ~Nov 2026 (was "July" — date drifted; users need months of data, not all of them).

[ ] Rate limiting — Cloudflare platform edge, per-IP, generous. All write endpoints + APPtrioc. Own conversation pre-Phase-5.

[ ] IANA timezone-name capture (v1.x). Includes the s29-parked `yesterday_iso` epoch−86400 DST edge (wrong ~1h around transitions for non-WA users) — same family, same fix window. (Both now recorded in the timezone doc, s32.)

[ ] Bonus pill two-sub ×1.5 display (s29 finding). Pill shows base multiplier only; server applies the sub boost invisibly. Lands with paywall/sub UI (5.s) — meaningless until subs exist.

[ ] Rotation/join TOCTOU returns 500 not 409 on a simultaneous-write race (s29 finding; pair_key UNIQUE keeps correctness). Cosmetic; note only — fix opportunistically if ever touching those handlers.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PARKED — known, not blocking
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] Rapid-tick untick under mock-clock hopping ONLY (s31; plain rapid ticking clean). s29 #7 residual — shared per-day flight flag/rollback namespace lacks request identity. Fix = per-request write identity in State, only if it ever bites without DevKit clock games.

[ ] Tasks tray resting size — smaller, more-square card. Independent of the lift (measures rect at runtime). Own checkpoint with before/after.

[ ] Bump wrangler `compatibility_date` (pinned 2026-01-01). Natural at Phase 5.

[ ] GOTCHA (s29): wrangler.toml top-level now mirrors DEV — bare `wrangler deploy` lands on the dev worker; prod REQUIRES `--env production`. Inverted from the old layout deliberately (accidental-deploy-to-prod was the dangerous direction). (Re-proven s36: bare deploy hit dev, --env production hit `four-tasks-api`.)

[ ] Windows curl TLS quirk (SEC_E_ILLEGAL_MESSAGE ~30min on fresh Worker URLs). Browser-test first.

[ ] `wrangler dev` drift to remote. Use `npx wrangler dev --local`.

[ ] Wrangler D1 token-refresh quirk. Fix: logout + login.

[ ] GOTCHA: autoload filename CASE must match registration exactly (Android PCK case-sensitive). AND (s32): case-only renames don't register in git on Windows (core.ignorecase) — use `git mv`.

[ ] GOTCHA: Android export needs INTERNET permission explicitly ticked.

[ ] GOTCHA: device-test via `adb install -r`, not email/download.

[ ] GOTCHA (s36): a STALE table survives `CREATE TABLE IF NOT EXISTS` — applying schema.sql does NOT fix a table created in an older shape. While a DB is empty, DROP TABLE <name> + re-apply. Once it has data, this is gone — use ALTER migrations (see ACTIVE schema-migrations). Also: `wrangler tail` logging an invocation as "Ok" means the handler returned a Response (incl. a handled 500) — it is NOT proof of HTTP 200; check the body/DB.

[x] ECONOMY REDESIGN — doc LOCKED s27 (AUTHORITY). Claim handler applies it in full as of s28. Amended s30 (streak/rescue) + s32 (span-sealing, 4.25 — doc-locked ahead of build).

[ ] Coin economy balance pass — feel tuning of placeholder figures against real play. rest_cost now server-shipped (s29) so retunes are one server constant; extend the economy payload container as more numbers go live (reroll cost, sticker prices).

[ ] Founders cutoff date locks (before Phase 5). Flag cutoff = first TestFlight/internal-track deploy; rate expiry = 31 Dec year 2.

[ ] Referral revenue-share (buddyware) design pass — own design note before build. Phase 5 commercial.

[ ] Pack drop content buffer — 3-4 packs queued before launch (half sub-only, half free). First sub-only ~2wk post-launch. Cadence under economy-doc review. (Delivery mechanism: server side LIVE as of s29 — tile 4.22; client popup + runtime art loading remain.)

[ ] Same-person multi-device pairing. Design needed only if multi-device sync becomes v1.x.

[x] Stamp messages authored — five tiers locked + LIVE dev/prod (s36). pickStampMessage returns real strings (claim-verified). Append-over-time, one-deploy. Optional further additions tracked in the roster-week content list.

[ ] Achievement framework — speculative. May never ship.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DEFERRED — v1.x or later
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] Push notifications (server-driven / FCM). Local scheduled reminders are v1.0 — tile 4.26.
[ ] Audio (silent v1, audio v1.1).
[ ] Partner reactions (deferred s12).
[ ] Leaderboard — KILLED for v1.0 (2026-06-10), removed from Phase 4. Eternal-stats optionality PRESERVED: lifetime_coins + longest_streak live in schema now; `four_tasks_tracking_design_notes.md` covers richer per-user stats if revived for experience depth later. Design docs stay in `four-tasks/deferred/`.
[ ] Streak popup + milestone bonuses (was 4.9; deferred s35). Streak bar number stays v1.0. Revive alongside the fuller milestone vision; red-penned message pool + proto mechanics (emoji escalation, every-5-days cadence) preserved as reference in stamp_streak_pools_working_draft.md; any bonus is a server-side payout component per the economy doc.
[ ] Coin name generator + reroll (schema columns reserved).
[ ] Per-user stat tracking beyond lifetime_coins/longest_streak.
[ ] TutorialCoordinator + staggered-disclosure reveals (was 4.19). v1.x. `users.tutorial_progress` column live.
[ ] Group / triple expansion. Possibly never. Pair-key §17.
[ ] Godot UID stability across machines (.gitignore vs *.import/*.uid, commit from canonical machine). Repo hygiene, do deliberately.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPLETED LEDGER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Full history with hours, commits, rationale: devlog v3 SUMMARY + v1/v2 archives.
Closed ad-hoc items through session 28 (separator change, accounted_for batch,
offset fix, _dict_equal non-bug, DevKit bounce stack, beat-collision lock):
see devlog — stripped from this file 2026-06-10 per lean-todo discipline.

Running project total at session 17 close: ~88.75h.
Running project total at session 23 close: ~108.75h.
Running project total at session 24 close: ~114.75h.
Running project total at session 25 close: ~121.75h.
Running project total at session 27 close: ~138.75h.
Running project total at session 28 close: ~142.75h.
Running project total at session 31 close: ~156.75h
(s29 ~5h · s30 ~5h · s31 ~4h; s29 figure is an estimate — correct if wrong).
Running project total at session 32 close: ~162.75h
(s32 ~6h ESTIMATE, doc-only incl. THEME PREP — correct at close if wrong).
Running project total at session 36 close: ~180.75h
(s33 ~7h · s34 ~3h · s35 ~3h · s36 ~5h — ALL ESTIMATES, FIVE corrections owed: s32-s36).
Running project total at session 37 close: ~185.75h
(s37 ~5h ESTIMATE — SIX corrections owed: s32-s37).