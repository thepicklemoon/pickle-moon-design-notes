FOUR TASKS — Godot Devlog (v4)
==============================
Last edit: 2026-06-20 AWST (session 44)
Last updated: session 43 — FOUR threads, all DEV. (1) 4.SH shop POLISH: the
preview backdrop went from a calendar-only clone to a FULL silent dupe of the
you-panel (PanelHeading + StreakBar + a static-open TasksTray now fill the slots
the retint pipeline will skin); the SAME one-time mouse_filter=IGNORE on the whole
PreviewLayout subtree CLOSED the s42 live-cell month-nav bug. (2) Shop rail origin
moved to the hamburger line (safe_top_inset()+24). (3) Sticker NAMING CONVENTION
locked: snake_case, id BYTE-IDENTICAL across folder / <id>_sticker.png / catalogue
sticker_id (camelCase was the import-miss root cause); add_stickers.sql adds four
catalogue rows. (4) 4.D SELECTOR reskinned from a centred modal to the RIGHT-edge
mirror of the shop rail — top leader DUPE = open/close, rail = owned stickers
(incl. leader) with tap->crown context menu (sets leader LIVE) + long-press pool
toggle; App passes the slot's global rect so the dupe parks over it. Threads 1-3
device-VERIFIED by Morgan; the selector reskin device-VERIFIED s44 (all six
checks). Session 43 time: ~5h. Session 44 (~1h): selector verified, W21-W23
wire pack (s41 shop/seal endpoints) added + dev-passed.
Engine: Godot 4.6
Studio: THE PICKLE MOON
Target stores: Google Play, Apple App Store

This is devlog v4 (live). Prior files are archive-only — read them for
historical reconstruction of HOW a decision was reached, not for the
decision itself:
  - `archived/four_tasks_godot_devlog_session_1-9.txt`     — sessions 1-9
  - `archived/four_tasks_godot_devlog_session_10-25.txt`   — sessions 10-25
  - `four_tasks_godot_devlog_v3_sessions_26-37.txt`        — sessions 26-37 (repo root)

This live file carries: the carried-forward SUMMARY (baseline state, last
fully refreshed at session 25 close — still the architectural baseline,
read it for the invariants) + sessions 38 onward. Design docs (in
`four-tasks/` under `pickle-moon-design-notes`) are the authority for
everything locked. Append new sessions after the latest.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AI READING GUIDE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Read this devlog before any code. The summary block is the entry
point; sessions append after it. Design docs carry the detail and
the authority — the devlog/todo are lean pointers, not archives.

Key locked design docs (authority):
  - architectural_preference          — clarity over cleverness (meta-rule)
  - pair_key                          — three-identifier model (user_id /
                                        pair_id / pair_key)
  - partner_reactions                 — field write rules (DEFERRED to v1.x)
  - staggered_disclosure              — day-2 reveals, gesture dismissal
  - morning_sequence                  — 12-beat ceremony + post-sequence
                                        presentation queue (collision resolved
                                        session 26)
  - economy_redesign                  — coin/streak/rescue AUTHORITY (any
                                        number or multiplier build reads it)
  - recovery / timezone_and_sealing / motd / stamp_tier / theme / week_mode /
    rate_limiting / tracking / onboarding / content_authoring_tool
  - wire_regression_pack.md           — re-runnable server wire tests (s32)

Repo map: `pickle_moon_repo_index.md` (raw-URL resolver). Always use
`refs/heads/main/` raw-URL form.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SUMMARY — STATE AT SESSION 25 CLOSE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ARCHITECTURE (locked):
  - Server is source of truth: Cloudflare Worker + D1 (Oceania), TS.
    Three-worker target dev/staging/prod. Godot client mirrors, polls,
    optimistic local writes w/ rollback.
  - Identity: user_id (person, forever) / pair_id (relationship) /
    pair_key (recovery hash). Self-writes only for v1.0; partner
    reactions deferred to v1.x.
  - HTTP envelope {ok,data}|{ok,error,message}; codes 200/400/404/409/500
    (+429 rate-limit only). No 401/403 (perm-denied folds to 404).
  - Sealing: LAZY walkback. Days sit unsealed; the morning claim
    (POST /users/:id/claim) walks back, seals the most recent unsealed
    accounted-for day, computes stamp/coins/streak then. `morning_payout_
    due_for` column dropped as redundant. (Eager-seal-at-completion
    considered + REJECTED session 26 — unnecessary over-engineering;
    lazy is sufficient. 4.25 span-sealing DOC-LOCKED s32: the walk will
    seal the whole span back to the last seal, not one target.)
  - File naming: autoloads PascalCase (Backend/State/Identity); else
    snake_case; internal code always snake_case.

PHASES:
  - Phase 1 (backend), Phase 2 (calendar/tray/swipe/poll), Phase 3
    (identity/onboarding/pairing/recovery) — CLOSED.
  - Phase 4 = ALL-CODE skeleton (build now, stub art). Phase 4b = painting
    only (4.A-4.F). Phase 5 = store submission.

SHIPPED THROUGH SESSION 25:
  - Full calendar + tray + two-panel swipe + poll-merge. Task list,
    bonus pill, streak bar. Onboarding (path choice, name, avatar,
    companions shell, MOTD placeholder, tasks). Recovery (3.10B)
    prod-verified end-to-end. PHASE 3 CLOSED.
  - 4.1 glitch typewriter (GlitchType.gd autoload + glitch_reveal.gd
    worker). MOTD spinner widget built standalone (msg_picker/msg_wheel).
    Companions screen (5b shell) — commits leader + 5 to active_stickers.
  - DevKit: always-on overlay, dev worker (four-tasks-api-dev) + dev D1,
    OS.has_feature("devkit") flag. Scenarios reboot through Loader.
  - Server: /resolve_solo, /motd/pool, /motd/roll, active_stickers
    persistence fix — all deployed dev+prod.

KEY TERMS (do not collide):
  - STICKER = calendar cell completion mark (active_leader art). Fires as
    its OWN hero moment when the 4th task is ticked — NOT part of the
    morning sequence.
  - STAMP = task-tray sealed-day artefact (tier-coloured + message).
    Belongs to the morning sequence (beat 7) and persists in the tray.
  - active_leader = identity-hashed visual sticker (was emoji/icon).
  - MOTD = the day's message; lives in header + that day's tray subtitle.
    Stamp message ≠ MOTD (two strings, two pools).

CARRIED NOTES / OWED:
  - Hand-written scene UIDs on newer scenes — delete .godot/ if a panel
    mis-renders on fresh import.
  - msg_wheel.gd integer-division warnings + dead var — cosmetic cleanup.
  - Spinner tuning deferred (column widths, font, doubled selector band,
    EMOJI REEL renders sticker IDs as TEXT not art — wheel is Label-based,
    needs a TextureRect column).
  - Dev D1 carries test cruft (TestUser/TestPartner + stale rows) — not
    cleaned, not blocking. Prod wiped to zero post-recovery-testing.

Running total at session 25 close: ~121.75h.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## Session 38 — 4.D2 week mode: server + read-side client
Time: ~Xh ESTIMATE.

★ PILLAR CORRECTION (Morgan, this session). Week mode is NO LONGER
"personal / not shared." The partner panel must be a FAITHFUL
RECONSTRUCTION of the partner's own calendar — their default,
overridden, AND sealed-immutable days, rendered exactly as they
appear in the partner's app. This is a hard reversal of the locked
week-mode doc, which argued privacy in five passages (those are now
WRONG — rewrite owed). The "no cross-user reads in v1.0" line is
DEAD. Partner reactions remain scope-cut; calendar mirroring is a
v1.0 pillar. Surfaced when Morgan, mid-build, stated partner
calendar fidelity is non-negotiable; the privacy framing was an
earlier-session decision that didn't reflect this.

What the mirror actually requires, per day, per slot:
  - sealed day      → partner's frozen days.sealed_labels (already
                      rode the partner payload — faithful already)
  - live, override  → partner's standard four ⊕ partner's weekday
                      override (THE gap this session filled)
  - live, no overr. → partner's standard four (task_list was already
                      partner-aware via _resolve_task_labels)

SHIPPED — SERVER (index.ts, schema.sql; dev deployed + verified):
- schema.sql: user_weekday_overrides +active_mask INTEGER NOT NULL
  DEFAULT 0 (bit i = slot i active). week_mode_weekdays comment →
  RETIRED-DEAD. sealed_labels comment corrected (s37 reset, not a
  "migration 001" — there is no migration regime yet). Both DBs
  reset (reset.sql + schema.sql; reset-not-migrate, no real pairing).
- PUT /users/:id/weekday_overrides/:weekday — self-write, whole-row
  idempotent upsert {task_labels[4], active_mask 0-15}. Router match
  placed BEFORE the bare PUT /users/:id catch-all + the day route.
  INVARIANT: every slot always NON-EMPTY — validateTaskLabels (the
  same helper the standard four uses) rejects empties, so a paused
  slot keeps its text with its mask bit cleared and a never-authored
  slot carries the standard-four placeholder; the mask ALONE decides
  active/inactive. Stronger than the doc's "normalise empty active →
  inactive": empties can't enter at all.
- GET /users/:id payload now ships weekday_overrides (self) AND
  partner_weekday_overrides (the mirror). fetchWeekdayOverrides is
  generic by uid, called for both sides of the pair; solo branch
  sends []. (Earlier this session it was briefly self-only with a
  "structural partner exclusion" — reversed wholesale by the pillar
  correction.)
- 9/9 wire tests green on dev: round-trip write, GET-carries-it,
  upsert-overwrites-no-dupe, mask>15 → 400, wrong label count → 400,
  empty-string label → 400 (invariant proven), weekday out of range
  → 400 (router), partner mirror (her override in YOUR payload).

SHIPPED — READ-SIDE CLIENT (device-verified):
- CalendarMath.iso_weekday(date) → 1=Mon..7=Sun (matches the
  override column convention; 0 on malformed → no match).
- State: self_weekday_overrides + partner_weekday_overrides, keyed
  by uid. State.resolve_labels_for_day(uid, day_row, date) is now
  THE single authority for week-mode label resolution (sealed
  snapshot → override bit-set+non-empty → standard four, per slot),
  consumed by BOTH the live render and the seal-snapshot so they
  cannot drift. Helpers _overrides_for / _override_row_for /
  _standard_four / _sealed_labels_of, all uid-keyed (partner
  resolves against partner data — the mirror).
- ★ SEAL RETROFIT DONE: _apply_seal_to_day now snapshots the
  RESOLVED four (resolve_labels_for_day for the sealing day's
  weekday), not the flat standard four. _self_labels_snapshot
  DELETED (superseded). This is the retrofit owed from s37 — it
  became load-bearing the moment overrides went live, because a
  wrongly-frozen own-day would then mis-mirror on the partner panel.
- task_list._resolve_labels_for_day delegates to State; task_list's
  own _resolve_task_labels + _extract_sealed_labels DELETED (logic
  centralised; file shrank ~47 lines).
- Backend.write_weekday_override + weekday_override_written/_failed
  signals (for the s39 editor).

BUG FOUND + FIXED (this session, on device): the partner's override
rendered on neither boot nor poll at first. Cause: I updated the
boot ingest (_on_user_loaded) but the periodic POLL uses a separate
handler (_on_poll_completed, a selective merge) that I'd missed —
it ingested self/partner/pair/days/economy but NOT the override
arrays. So overrides could survive a cold boot and then get dropped
on the very next poll. Fixed: _on_poll_completed now ingests both
override sets (server-authoritative, take-as-is, same as boot).
After the fix: partner's overridden Wednesday slot 3 reads "HER WED"
while the other three show her standard four — the mirror confirmed
on screen.

Lesson (logged): when adding a server field the client must hold,
EVERY ingest path needs it — boot (load_user → _on_user_loaded) AND
poll (poll_completed → _on_poll_completed). They're separate
handlers by design (full reset vs selective merge); a field added to
one silently dies on the other.

Also surfaced (→ todo, launch-hardening, NOT this tile): the client
conflates "network unreachable" with "server returned 404 for my
stored user_id." A DB reset wipes the server but the device keeps
its saved id, so boot calls load_user(saved_id), gets nothing, and
parks on "Couldn't reach the server. Tap to retry." — which is why a
reset does NOT route to onboarding (onboarding is gated on local
identity absence, not server absence). Harmless in dev (re-seed) but
in prod a deleted/dangling account traps a real user on infinite
retry. Fix before launch: distinguish 404-on-load (→ onboarding /
"account no longer exists") from a real network error (→ retry).

OWED:
- AUTHORING UI (4.D2 remainder, s39): override editor modal (a
  SECOND modal cloned from the tasks_edit_dialog scaffold — checkbox
  column + pause semantics + the new endpoint; NOT a mode-flag on the
  standard-four dialog, against clarity-over-cleverness); weekday-
  HEADER long-press → opens it scoped to that weekday (the MON-SUN
  header row ALREADY EXISTS in calendar_grid.tscn as static labels —
  it is NOT a strip to build; wire a long-press onto the labels);
  active-override weekday header BOLDED (treatment tunable later);
  hamburger-editor banner gated on today's weekday having an active
  override.
- WEEK-MODE DOC REWRITE: kill the 5 privacy passages, replace with
  the faithful-mirror pillar. Pair-key section stays correct
  (overrides are visible but do NOT participate in pair-key hashing).
- index.ts → PROD: now carries 4.SL seal-write AND 4.D2. npx
  wrangler deploy --env production (from server/). Prod schema add
  is still a reset (no real pairing yet).

NEXT: 4.D2 authoring UI (the editor modal + header long-press +
bold + banner), then the doc rewrite, then the prod deploy.

Session 38 time: ~Xh ESTIMATE.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## Session 39 — 4.D2 week mode authoring UI + sealed-day immutability
Time: ~5h ESTIMATE (correct at close).

Closed 4.D2. s38 shipped the server + read-side mirror; this session
built the authoring UI on top, then spent most of its length hunting —
and finally fixing — a sealed-day immutability break that the new
override layer exposed. MODEL A locked (Morgan): a sealed day PRESERVES
its override; the snapshot IS the memory.

SHIPPED — authoring UI (client):
- weekday_edit_dialog.gd (NEW, all-code modal, cloned from the
  tasks_edit_dialog house pattern — scrim 0.6 + centred card). Four
  [checkbox][LineEdit] rows: inactive shows the standard label greyed +
  locked; tick → editable; untick → PAUSE (typed text preserved for a
  re-tick); empty-on-blur → revert to inactive. Save builds the server
  invariant (4 non-empty + mask) and calls the optimistic write. A
  zero-ticked save writes an all-inactive row = CLEARS the weekday's
  override (the un-set path). NO today-disarm (deemed overkill; known
  accepted seam: tick today, override that slot, untick → flips to the
  override, because an unsealed day resolves live).
- State.request_weekday_override(weekday, labels, mask) — OPTIMISTIC:
  find-or-insert the local row, emit self_changed for instant repaint,
  write via Backend, roll back the WHOLE override array on fail. New
  signal weekday_override_failed. Snapshots the whole array (uniform
  insert/update rollback; ≤7 tiny rows — clarity over cleverness). No
  in-flight guard (a mid-write poll stomp self-corrects sub-second, same
  accepted risk as self_user).
- calendar_grid.gd — WeekdayStrip (the MON-SUN static labels that already
  existed in calendar_grid.tscn) long-press HOSTED in the grid: timing in
  _process, drag-cancel past TAP_DRAG_THRESHOLD, LONG_PRESS_DURATION;
  emits weekday_long_pressed(1-7). SELF-GATED via _is_self() (user_id ==
  self_user.user_id) — connections are made on both grids but the handler
  no-ops on the partner grid, so the partner strip is inert. Active-
  override header BOLD = colour step TEXT_WEEKDAY → TEXT_HEADER (no bold
  font variant in Fonts.gd; tunable later, Morgan's call).
- App.gd — opens the editor on weekday_long_pressed, same modal hygiene as
  the hamburger (ceremony-inert, one editor at a time, cross-guarded vs
  _tasks_edit). tasks_edit_dialog.gd — banner when today's weekday has an
  active override (edits there reach only the non-overridden slots).

THE IMMUTABILITY FIX (the session's hard problem):
- BUG: a sealed day showed the LIVE override (e.g. "dsfaf" on a sealed
  past Wednesday). The new override layer exposed a latent gap.
- ROOT (two, conflated): (1) the resolver decided "is this day frozen?"
  by SNAPSHOT-PRESENCE (sealed_labels is 4-elem), not by SEALED-NESS
  (sealed_at != null). A sealed day with an empty '[]' snapshot — a row
  sealed before the sealed_labels column existed, a seed-gap row, or a
  PHANTOM rowless past day (getUserPayload ships every day from
  {year-1}-01-01, so old test rows land in State.days with no real row) —
  fell through to the override. (2) the unsealed/override branch had NO
  past-guard, so today's override also leaked onto unsealed/phantom PAST
  days.
- FIX (State.resolve_labels_for_day rewritten, for_seal:bool=false param):
  (1) gate on sealed_at — a sealed day returns its snapshot, or the
  standard four if the snapshot is empty, but in NO case the override.
  (2) DISPLAY-only past-guard — a past unsealed/phantom day shows the
  standard four (no backward leak); normal operation never hits this (the
  claim seals past days) so it only catches anomalies. (3) for_seal flag —
  _apply_seal_to_day passes true, which SKIPS both the sealed-branch (the
  row is still unsealed at freeze time — we're minting its snapshot) AND
  the past-guard (a walkback day DID live under the override → its memory
  should keep it). MODEL A.
- _apply_seal_to_day REORDERED: resolve the labels WHILE the row is still
  unsealed, THEN stamp sealed_at — else the new sealed-gate would take the
  frozen branch, find no snapshot yet, and freeze the standard four,
  dropping the override.
- SERVER mirror (index.ts): resolveSealedLabels(standardJson, overrides,
  date) + isoWeekdayOf(date) added — the seal snapshot is now the RESOLVED
  four per-day (was the standard four computed once before the loop, the
  4.SL retrofit owed since s37). Applied at ALL THREE seal sites: the
  sealSpan loop, and the gap-rescue breaker INSERT in resolveClaim
  (handledExternally bypasses the loop, resolved separately). Grey-rescue
  rides the loop. devkitWipeStatements already clears the override table,
  so the seed backfill (standard four) stays correct — no seed change. The
  server does NOT past-guard (every sealSpan day is past by construction;
  guarding would mean overrides never freeze, which Model A rejects) — it
  matches the client for_seal path so the two snapshots agree.

VERIFIED (device):
- Editor: long-press a weekday header on the SELF panel opens it;
  activate/type/save → live override renders + header bolds; pause/re-tick
  restores text; empty-on-blur reverts.
- Immutability: phantom past days now show the standard four (was the
  bug); real seeded sealed days show their snapshot.
- SEAL-THROUGH (the gate Morgan wanted closed before dogfooding): set an
  override on today's weekday, DevKit "Today +1 day" → bg/fg the app →
  the real claim walked back and sealed today via sealSpan. DB confirmed
  the frozen snapshot: sealed_labels = ["asdaw","walk 20 min","read a
  chapter","fsadf"] — MIXED per-slot freeze (slots 0,3 overridden; 1,2
  standard). Proves resolveSealedLabels' per-slot mask logic AND that the
  client and server snapshots agree (held through the beat-12 poll, no
  flicker). This was the path s38's seed tooling couldn't produce —
  reached via the mock-clock, not a reseed (the morning-payout scenario
  wipes the override).

PROCESS NOTE (cost ~3 false bug-hunts before the real fix landed):
autoloads do NOT hot-reload. State.gd edits require Project → Reload
Current Project (NOT stop-scene) before they're live — a running editor
keeps the old singleton in memory. The "deployed and still broken"
symptom across multiple attempts was a stale on-disk State.gd the whole
time; a server deploy can't change a client-side label resolve. Lesson:
when something is "deployed but still broken," disk-verify the CLIENT
file (Select-String for a unique token) and ground-truth the DB FIRST,
theorise after. Three correct fixes sat un-run because the file wasn't
live. Adding a boot version stamp was floated and declined (would be
forgotten).

NOT DONE (carried — NONE block dogfooding except the prod deploy):
- index.ts → PROD + prod schema RESET (reset.sql then schema.sql,
  --remote --env production; prod has no real pairing yet so a reset is
  safe and is what adds the columns/tables). THE DOGFOODING GATE — until
  this ships, a prod seal freezes wrong labels / an override write 500s.
- Partner-render on PROD never exercised by two real users (built +
  verified solo via the dev seed's fake partner; first thing to watch
  when Morgan + Ayshe pair).
- WEEK-MODE DOC REWRITE: four_tasks_week_mode_design_notes.md still has
  the 5 "personal/not shared" passages (false since s38), and MODEL A
  currently lives ONLY in a State.gd code comment — it must become the
  doc's authority (incl. the for_seal display-vs-freeze split + why).

NEXT: prod deploy + reset (the gate), then Morgan + Ayshe dogfood on
prod. Then the doc rewrite. 4.FT fourth-tick seal still open —
_apply_seal_to_day is now the proven client seal path (resolves while
unsealed, then stamps); route the server trigger through sealSpan so it
inherits resolveSealedLabels.

Session 39 time: ~5h ESTIMATE (correct at close).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## Session 40 — MOTD sticker render (emoji slot → inline sticker art)
Time: ~5h ESTIMATE (feature build + a repo-recovery morning; correct at review).

The MOTD's emoji slot renders as the user's sticker ART everywhere it appears —
not the raw id-string. Full-stack, four stages, on the make-the-data-say-what-
it-means rule: motd_sticker is its OWN stored field, never recovered by parsing
the message string.

SHIPPED — STORAGE (schema.sql, index.ts):
- schema.sql days: +motd_sticker TEXT NOT NULL DEFAULT '' (frozen at seal
  alongside motd — the snapshot is the memory, same model as labels).
- index.ts: motd_sticker validated + persisted in createUser day-0 INSERT and
  writeDay UPDATE (NFC+trim, ≤64, '' default); carried in the claim/presented
  ceremony payload. parseDayRow spreads ...row, so poll + fetch return it with
  no per-endpoint mapping — the field rides the general serializer.

SHIPPED — CLIENT:
- stickers.gd (NEW, class_name): static texture_for(id) → res://assets/icons/
  <id>/<id>_sticker.png, ResourceLoader-guarded, null-on-miss + warn-once +
  success-cache. The single sticker-texture authority.
- msg_picker: composed_text() (3 word reels) + selected_sticker() (emoji reel
  id) exposed SEPARATELY; both commit points (onboarding_motd + morning
  _commit_today_motd) write both via State.set_motd(date, motd, motd_sticker).
  composed_message() (the old joined string) DELETED.
- msg_wheel: RenderKind {TEXT, STICKER} — emoji reel builds a TextureRect per id
  (unpainted → Label fallback); ALL spin/drag/snap/wrap motion math untouched
  (it works in rows, agnostic to content).
- motd_sticker_slot.gd (NEW, class_name): wraps a subtitle Label in a tight HBox
  + trailing sticker; reads the label's own pack flag so it matches the centred
  header AND the left tray with no special-casing. The label keeps driving its
  text (incl. the glitch); the slot only owns the sticker.
- panel_heading + tasks_tray: render text+sticker from the SAME row (header
  walk-back = today's-or-most-recent-prior; tray per-day). morning_sequence sets
  the sticker WITH the beat-7 (tray) + beat-10 (header) glitches so it rides in,
  not pops at release; App passes the header slot through run().

NEW FILES (hard deps — nothing parses without them): stickers.gd,
motd_sticker_slot.gd.

VERIFIED (device): the picker reel renders the emoji as sticker art (the s36
"spinner shows words" gap is closed by the stage-3 TextureRect path).

UNVERIFIED: header subtitle, sealed tray, ceremony beats 7/10. They showed a
split-second-then-gone — SCHEMA DRIFT, not code: the deployed DB lacked
motd_sticker, so every poll round-tripped a row without it and the selective
merge took the full server row, wiping the local optimistic value. Resolved by
the reset below; wants a fresh-ceremony device pass to close.

SCHEMA / DEPLOY (the s39 dogfooding gate — DONE): prod + dev D1 reset (reset.sql
then schema.sql, --remote) + both Workers redeployed — folds the s39 4.SL + 4.D2
server AND the s40 motd_sticker column. Prod re-onboard + re-pair pending (reset
wiped dogfood data). Gotcha logged: CREATE-TABLE-IF-NOT-EXISTS won't add a column
to a live table — the reset applies it (ALTER TABLE ADD COLUMN is the non-
destructive keep-data alternative).

REPO RECOVERY (housekeeping, logged for the time it cost): a VS-Code-sync + two-
machine divergence orphaned the good commit; recovered via reflog to 97f86fb
(week mode + stickers both present, git-confirmed) + force-pushed as source of
truth. Re-confirmed: pull before work / push after, per machine; the Sync button
MERGES — the terminal force-push is the source-of-truth hammer.

OPEN (carried):
- DEVICE-VERIFY the render surfaces (header/tray/ceremony) on a fresh MOTD pick —
  the test-ledger gate (reel already green).
- WEEK-MODE DOC REWRITE — still owed s38/s39 (5 stale privacy passages; Model A
  still only in a code comment).
- 4.FT fourth-tick seal still open.
- onboarding companions inline load() → fold onto stickers.gd.
- Launch-hardening: 404-on-load vs network-error (a reset/dangling account traps
  a real user on infinite retry).

NEXT: device-verify the sticker render surfaces, then stand up the Play Store
internal-testing track — the new priority, for OTA patching now that dogfooding
is live (primer: four_tasks_play_store_testing_primer.md). Then the week-mode
doc rewrite.

Session 40 time: ~5h ESTIMATE.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## Session 41 — Completion-seal + sticker pool + shop (server tile + client read + selector)
Time: ~7h ESTIMATE (full server tile + wire test + client read path + selector/leader slot + 3 regression fixes; correct at review).

The fourth tick now seals the day's CONTENT and freezes a sticker; the cell +
hero render that real sticker; a catalogue/owned/pool model backs a working
shop and a leader-slot selector. Everything DEV-only, server side wire-proven
end to end. ALL ON THE dev DB + dev Worker — prod is untouched (it holds the
Morgan+Ayshe dogfood pairing; deploying here means a reset, deferred).

DESIGN — TWO-SEAL MODEL (four_tasks_completion_seal_and_sticker_pool_design_
notes.md, NEW — authored this session, NOT yet committed to the repo / index):
- "Seal" splits into TWO events. (1) COMPLETION seal at the FOURTH TICK:
  content locks, a sticker is PICKED from the pool and FROZEN into
  days.completion_sticker, immutable instantly (survives overnight / unpair /
  unsub). The hero fires here. (2) ACCOUNTING seal at the morning claim:
  payout/streak/stamp/sealed_at/day_theme_state — the existing sealSpan,
  UNCHANGED. Rationale: the claim/span/rescue machinery rests on "today never
  seals" (buildSpan upper bound date < localDate STRICT); making the 4th tick
  set sealed_at would mean rewriting that fragile walkback. So completion is a
  SEPARATE lighter mechanism (a new column + edit rejection), not sealed_at.
- PICK = DERIVE-then-FREEZE. stable_hash(date) % pool.length indexes
  active_stickers, deterministic per date, then frozen. NOT pure re-derive
  (that would reshuffle past days when the pool changes, breaking immutable-
  past). Sticker identity FREEZES (its input, the pool, is mutable); placement
  RE-DERIVES (its input, the date, is immutable). The two are different on
  purpose.
- NO morning promotion. completion_sticker is a PERMANENT column the seal never
  touches — already the forever home. The client reads completion_sticker for
  ALL days (one source). An earlier draft had a day_theme_state promotion step;
  CANCELLED as redundant (verified sealSpan never writes the column).

SHIPPED — SCHEMA (schema.sql, applied to dev D1 via reset+reseed):
- NEW `stickers` catalogue table (sticker_id PK, display_name, price_coins
  DEFAULT 1, subscriber_only, ships_extra, status 'live'/'queued', released_at)
  — the source of truth for buyable stickers + prices, NOT content_drops. 11
  baseline rows seeded, matching the client onboarding CATALOGUE const exactly
  (black_cat, blue_swamplight, chilli, fly, frog, green_glowbug, grey_cat,
  orange_swamplight, skemli, vampire, water_bottle).
- days: +completion_sticker TEXT DEFAULT '' (the frozen pick; immutable once
  set; the seal never touches it).
- users: +owned_stickers TEXT DEFAULT '[]' (entitlement) — distinct from
  active_stickers (the selected pool that feeds completion picks + the MOTD
  reel). Selected ⊆ owned.

SHIPPED — SERVER (index.ts, dev Worker; WIRE-PROVEN):
- writeDay: on the write that takes a day to 4 ticks, pickCompletionSticker
  freezes completion_sticker. Content-lock rejects further edits 409
  day_complete (detects via existing completion_sticker OR 4 stored ticks).
  stableHash ported to TS (Math.imul / >>>0) — VERIFIED byte-identical to the
  GDScript hash across dates.
- setRestDay: also 409 day_complete on a completed day (the second edit path —
  a four-tick worked day can't toggle to rest).
- Shop: GET /users/:id/shop (listShop — live catalogue minus owned) + POST
  /users/:id/shop/buy (buySticker — verify coins, atomic relative debit +
  append to owned+active, 409 already_owned / insufficient_coins / 404
  sticker_not_found).
- Selector writes: NO new endpoints — PUT /users/:id (updateUser) ALREADY
  handles active_leader (w/ pair-key rotation) + active_stickers.
- BUGS found via wire test + fixed: parseUserRow now parses owned_stickers
  (was returning a raw JSON string → client would choke); createUser + BOTH
  DevKit seeds now populate owned_stickers = the starter active_stickers (the
  starter set is OWNED, not just selected — closes Morgan's "stickers living
  outside the ecosystem" flag; onboarding flow UNCHANGED, the granted stickers
  are now first-class catalogue members).

SHIPPED — CLIENT (read path, tile 4.D):
- day_cell.gd: configure(d, s, sticker_id); the sticker is driven by
  completion_sticker != "" (truer than the GREEN tier — fires at four ticks
  regardless of seal, and correctly shows on a sealed past day); real art via
  Stickers.texture_for, placeholder star = explicit art-missing fallback; only
  the monochrome star is tinted, never real art.
- calendar_grid.gd: passes completion_sticker into configure via
  _completion_sticker_for(date) (reads the day row it already fetches).
- hero_beat.gd: play(from_rect, date, kind, sticker_id); real art + star
  fallback; tints star only.
- App.gd: hero RE-DERIVES the pick locally (CalendarMath.pick_completion_
  sticker, mirrors the server pick exactly — verified gives "chilli" for
  2026-06-18, matching the wire test) so it lands INSTANTLY at the 4th tick,
  BEFORE the server's completion_sticker round-trips. The permanent cell still
  reads the server's frozen value (source of truth). Worked-only (rest carries
  no completion sticker).
- State.gd: _new_day_row template carries completion_sticker; _on_day_written
  INGESTS it (server-controlled field) — the "ingest every new server field in
  BOTH boot and write/poll handlers" footgun, hit again and closed.

SHIPPED — CLIENT (selector, tile 4.D — the real interface, not a stub):
- leader_slot.gd (NEW, class_name): top-right leader-sticker button MIRRORING
  the hamburger (same SIZE, same top inset, gesture vocabulary). Long-press
  opens the selector; tap inert. Repaints its art live off State.self_changed.
- sticker_selector.gd (NEW, class_name): vertical owned-sticker list (scrim +
  centred card, house modal pattern). Each row: art, name, tap-to-set-leader,
  pool toggle. Owns its OWN writes via State.request_user_update (no App
  handler needed — no coordination App must own, unlike rest_confirm).
  Refreshes on self_changed. Empty-pool guard: refuses to remove the last pool
  member (a zero pool would starve completion picks + the MOTD reel).
- App.gd hosts the leader slot + selector lifecycle (NOT DevKit — an earlier
  pass wrongly stubbed it in the DevKit menu; corrected to the real top-right
  slot + long-press).

NEW FILES (hard deps): sticker_selector.gd, leader_slot.gd.

VERIFIED (device): the cell renders the real frozen sticker (chilli, hand-
placed, persists across mock-forward+reload — proving the State ingestion, not
just the local prediction); the hero lands the real sticker INSTANTLY at the
4th tick (after the re-derive fix; before it, the sticker only appeared on a
later reload because the hero read State before the write response landed); the
leader slot shows the leader top-right and long-press opens the selector; set-
leader repaints the slot live; pool toggle changes which sticker future
completions draw from.

VERIFIED (wire, dev Worker — full sequence): GET /shop → 11, then 7 after
owned populated; 4-tick today → completion_sticker:"chilli" with sealed_at:null
(two-seal confirmed); 5th edit → 409 day_complete; rest toggle → 409
day_complete; buy green_glowbug → coins 4250→4249, added to owned+active,
dropped off buyable; re-buy → 409 already_owned; owned_stickers returns as a
parsed array post-fix.

PROCESS NOTE: a phantom parse-error storm (calendar_grid "Cannot find member
CellState") was the class_name-not-reregistered footgun AGAIN — a parse error
makes the whole file's symbol references report at the USE site, and a stale
class_name (day_cell defines DayCell) cascades to every referencing file.
Reload Current Project cleared it; nothing was wrong with the files. Same
lesson as s39's autoload reload: after dropping a class_name script, reload
before trusting any error.

PER-DATE DETERMINISM (parked, not a bug): the sticker is deterministic per
date by design (re-derive-never-remember). Re-ticking the same date always
gives the same sticker in the same spot — that IS the invariant (immutable
past). With only a 4-sticker starter pool it reads repetitive; resolves as the
catalogue + pool grow. Morgan parked it ("until it casts a PROPER shadow").

OPEN (carried):
- THIRD swipe panel + sticker SHOP — the next build. App.gd's 2-panel swipe
  becomes 3-panel (container 3-wide, current_view third state, commit
  thresholds third target — a real swipe-state-machine change, costed). Then
  populate from GET /shop, tap→buy. Preview window DEFERRED (needs the palette/
  retint pipeline, 4.14b/4.20). OWN FOCUSED SESSION — invasive on core nav.
- PROD deploy of the s41 work — dev-only now; prod reset required (holds the
  dogfood pairing). Sequence with the other pending prod work.
- COMMIT the design note four_tasks_completion_seal_and_sticker_pool_design_
  notes.md to pickle-moon-design-notes + add to the repo index (currently only
  a working file, never committed).
- Q6 (deferred): onboarding's free allocation vs the paid shop AT CATALOGUE
  SCALE. Onboarding picks ~5 free from the full catalogue (fine at 11; at the
  planned ~100, "pick 5 free from 100" guts the cheap shop + bloats the
  screen). Likely a `starter_eligible` flag + a fixed small free allocation.
  NOT a now-problem; flow stays as-is.
- WEEK-MODE DOC REWRITE — still owed s38/s39/s40.
- 4.FT fourth-tick seal still open (NOTE: the COMPLETION seal shipped here is
  the content-lock + sticker freeze; 4.FT is the morning ACCOUNTING-seal hero
  beat, separate).

NEXT: the third swipe panel armed with the sticker shop (own session — swipe
surgery). Then prod deploy + reset to fold s41. Then the week-mode doc rewrite.

Session 41 time: ~7h ESTIMATE.

Running total at session 41 close: ~207.75h
(s40 ~5h + s41 ~7h, BOTH ESTIMATES. Builds on the s39-close ~195.75h. The
EIGHT-PLUS time-debts since s32 are still a smell — one reconciliation pass
owed, now spanning s32-s41.)

## Session 42 — Tile 4.SH: the sticker shop as the third swipe panel (client)
Time: ~6h ESTIMATE (plumbing + the 2→3-panel swipe surgery + three layout reverts; light session, correct at review).

Shipped the client half of tile 4.SH: the sticker SHOP as a third swipe
panel (SHOP ← calendarYOU ↔ calendarPARTNER). Server was already done +
wire-proven (s41); this session is entirely client. The shop SPINE is
complete and device-verified end to end; the shop's VISUAL polish + the
sticker-preview render are explicitly deferred (4.SH ships PARTIAL).

DESIGN DECISIONS LOCKED:
- DECISION B — the shop panel OWNS its catalogue locally; State does NOT
  hold it. Nothing else in the app reads a "buyable list", so parking it
  in State would carry transient panel state in the global store. The panel
  fetches via State.request_shop_list(), catches the live catalogue on
  shop_list_ready, stores it, and derives BUYABLE = catalogue MINUS
  self_user.owned_stickers on every repaint. That derivation IS the "bought
  sticker leaves the shop" mechanism — a buy grows owned, fires self_changed,
  the panel re-derives, the sticker filters out. No manual row removal, no
  re-fetch, no buy-success signal.
- INTERACTION: TAP a rail sticker = SELECT (preview hook, INERT this session
  — the calendar-clone preview RENDER needs the palette/retint pipeline,
  4.14b/4.20). LONG-PRESS = buy-confirm overlay → confirm → buy. Mirrors the
  house gesture vocabulary (Layout.LONG_PRESS_DURATION / TAP_DRAG_THRESHOLD),
  so a horizontal swipe begun on a rail sticker is neither tap nor hold — it
  falls through to App's panel swipe and the whole frame slides back to you.
- BUY is optimistic WITH the coins moved (unlike the rest toggle, whose coins
  can't be guessed up-front because a refund's sign is ambiguous). The price
  is known from the catalogue row, so the debit is exact: subtract price,
  append to owned+active, emit self_changed for instant feedback; the server
  response then supersedes the guess; rejection rolls the whole self_user back
  from a "shop|<id>" snapshot. Same accepted poll-stomp posture as the other
  self_user optimistic writes (the buy window is poll-stompable; the response
  resettles — sub-second against a 30s poll).
- LAYOUT (model B, the user's sketch): the preview calendar sits in the SAME
  dimensions as the real you/partner panels (full width), and the sticker rail
  is SUPERIMPOSED over its left as a translucent overlay. NOT a side-by-side
  width-split (tried, looked worse — see process note). The shop has no tasks
  tray, so a BottomSpacer fills the void the tray occupies on the real panels.
- DROPPED the ScrollContainer from the rail: a scroll region inside a swipe
  panel is inherent gesture contention, and the project already rejected
  intent-locking. The current buyable set (~7) fits a fixed rail; catalogue
  SCALE (the ~100 case) resolves via curation (featured/rotating), not a
  100-item vertical scroll — deferred, not this session's problem.

SHIPPED — BACKEND.gd:
- list_shop(user_id) → GET /users/:id/shop; buy_sticker(user_id, id) →
  POST /users/:id/shop/buy. Two signal pairs (shop_listed/shop_list_failed,
  sticker_bought/sticker_buy_failed). Uncached (a buy changes what's buyable;
  a cache would re-offer an owned sticker). Mechanical — mirrors set_rest_day.

SHIPPED — STATE.gd:
- request_shop_list() (thin forward) + request_buy(sticker_id, price)
  (optimistic self_user debit+grant, "shop|<id>" snapshot, rollback on fail).
  _buys_in_flight guard (sticker-id keyed, SEPARATE from the day-keyed
  _writes_in_flight the poll-merge reads). Re-broadcast signals shop_list_ready
  / shop_list_failed / sticker_buy_failed for the panel. A successful buy needs
  no shop-specific signal — it merges the server user and emits self_changed.

SHIPPED — shop_panel.gd + shop_panel.tscn (NEW, class_name ShopPanel):
- Two-layer scene: BACK = a calendar-clone (PreviewCalendar, a real
  calendar_grid.tscn instance bound to self) for preview backdrop; FRONT = the
  translucent rail overlay holding buyable stickers. Built as a SCENE (not
  code) because it embeds a real calendar_grid instance — a bare script can't.
- Inner class ShopRailItem (tap+long-press gesture detection, mirrors
  leader_slot). Inline buy-confirm overlay (self-contained, no App involvement,
  matches the selector's own-writes pattern; shows "can't afford" state).
- Selecting a sticker is the INERT preview seam (_on_sticker_selected) the
  retint pipeline plugs into; tracks selection + a selected ring for now.

SHIPPED — App.gd (the 2→3-panel swipe surgery — invasive on core nav):
- Named view constants VIEW_SHOP=0 / VIEW_YOU=1 / VIEW_PARTNER=2 /
  VIEW_HOME=VIEW_YOU. App boots showing calendarYOU (the MIDDLE panel): panels
  at local x 0/W/2W, container width 3W, container boots at -W. current_view
  defaults to VIEW_HOME.
- _setup_shop_panel instances the shop SCENE into PanelContainer at slot 0.
- Drag machine generalised: _end_drag steps one neighbour each way, clamped at
  both ends; _snap_to target = -W*view; the LIVE-DRAG CLAMP rewritten to
  intersect a per-drag adjacency bound (±1 panel from drag start) with the
  global edge wall ([-2W, 0] + give) — the old fixed two-panel clamp walled the
  finger immediately when dragging from the partner panel.
- _carry_open_day_across_panels GATED to skip the shop (the shop has no
  calendar/tray) and re-derived from MEANING, not the old 0/1 shorthand — under
  the new numbering 0 is the SHOP, so every old `== 0` ("the you panel") had to
  be rewritten to VIEW_YOU. The ceremony snap-home likewise: _snap_to(VIEW_HOME),
  NOT _snap_to(0) (0 is now the shop). go_to_view guard widened to 0..2.
- HAMBURGER + LEADER SLOT re-parented from the App ROOT onto PanelYou, so they
  travel WITH the you panel on swipe and are off-screen on the shop/partner
  panels (they were floating over every panel). Safe — the morning input
  blocker (z=200, App root) still covers PanelYou's children during the
  ceremony, and their handlers already gate on _morning_active.

VERIFIED (device, dev): app boots on your calendar; you↔partner swipe feel
UNCHANGED (incl. the open-tray day-carry across panels); swipe-right reaches
the shop, swipe-left returns; the buy loop works on the NEW long-press path
(long-press → confirm → coins deduct → sticker leaves the rail → appears in
the selector's owned list); hamburger + leader slot now ride PanelYou (gone on
shop/partner, back on you). Coins subtract correctly.

NOT VERIFIED / DEFERRED:
- Shop VISUAL polish — rail/sticker sizing, exact overlay tuning. Functional
  and readable but not final; the rail superimpose-over-full-calendar layout
  is settled (model B), fine-tuning of offsets/sizes is open.
- The SELECT→preview RENDER (the sticker painting onto the calendar clone) —
  the whole point of the two-layer structure, but BLOCKED on the palette/retint
  pipeline (4.14b/4.20). The seam (_on_sticker_selected) exists, inert.
- Preview-calendar cells are still live (tapping one off-rail can month-nav) —
  harmless, not gated; one-line follow-up if it bothers real use.
- PROD: still dev-only (carries from s41). Prod reset folds s41+s42 together.

PROCESS NOTE (the cost this session): the shop LAYOUT was iterated three times
the WRONG direction. The user showed a working version (full-width calendar +
superimposed rail = model B, their sketch). I argued into a side-by-side
width-split (model A) over a preview-COLLISION concern — but the preview render
isn't built, so there was nothing to collide. A made it worse twice (incl. an
anchors_preset=0 bug that collapsed the rail to a cramped box: a negative
offset_bottom measures from the TOP when anchor_bottom=0, not the bottom).
Reverted to B. LESSON: when the user has a working layout and the objection is
about a feature that doesn't exist yet, leave it alone — don't optimise a
solved layout against a hypothetical.

PROCESS NOTE (str_replace doubling): a str_replace doubled the _setup_shop_panel
body (two shop panels instanced → "one calendar across all three panels", on
top of an anchors=15 full-rect-stretch bug that smeared the shop across the
whole 3-wide container). Caught by a duplicate-line scan. Whole-file diffs
against the on-disk mirror after every edit remain the discipline that catches
these.

OPEN (carried):
- 4.SH PARTIAL — spine + swipe + rail SHIPPED + verified; visual polish + the
  select→preview render DEFERRED (the latter blocked on 4.14b/4.20).
- PROD deploy of s41+s42 — dev-only; prod reset required (holds the dogfood
  pairing). Sequence with the other pending prod work.
- Add the s41 shop endpoints to wire_regression_pack.md (still open from s41).
- WEEK-MODE DOC REWRITE — still owed s38-s41.
- 4.FT fourth-tick ACCOUNTING-seal hero beat still open.

REPO INDEX corrected this session (probe-verified against the live repo): the
devlog ARCHIVES live under archived/ as _session_1-9 / _session_10-25 (NOT
deferred/ with the _v1_sessions_ / _v2_sessions_ names the old index +
the devlog head both claim); v3 sits at four-tasks/ root. The seal/sticker
design note (committed since s41) added to the authority list. Three confusingly
-named folders flagged: archived/ (devlog rollovers) vs archive/ (one superseded
doc) vs deferred/ (v1.x/killed). Committed to the repo + Project copy.

NEXT: finish 4.SH's visual polish (rail/sticker tuning) if wanted, else it's
shippable-partial. Then either the prod deploy+reset to fold s41+s42, or the
week-mode doc rewrite (overdue). The select→preview render waits on the theme/
retint pipeline (4.14b).

Session 42 time: ~6h ESTIMATE (light session — plumbing + the swipe surgery +
three layout reverts).

Running total at session 42 close: ~213.75h
(s42 ~6h ESTIMATE. ELEVEN time-debts now owed since s32 — the reconciliation
pass is well overdue, spanning s32-s42.)

## Session 43 — 4.SH polish (full dupe backdrop + rail origin) · sticker naming convention · 4.D selector reskinned to the shop-mirror rail
Time: ~5h ESTIMATE (threads 1-3 device-verified by Morgan; the selector reskin is UNVERIFIED — verification firms the figure).

Four threads, all on DEV. Three verified on device by Morgan in-session; the
fourth (the selector reskin) ships UNVERIFIED.

SHOP PREVIEW PROMOTED TO A FULL SILENT DUPE (4.SH polish) — VERIFIED:
The preview backdrop was a calendar-only clone; now it's a full dupe of the
you-panel, so every surface the retint pipeline (4.14b/4.20) will skin is present
to preview against. shop_panel.tscn: ShopTitle -> PanelHeading (side=self),
StatusLabel -> StreakBar, BottomSpacer -> TasksTray, + a front-layer ShopStatus
float for the shop's own status line (the duped header title is DROPPED — it
collides). tasks_tray.gd gained show_static(date): the _animate_open content path
(header + task list + stamp) MINUS the tween, card left at its container-laid
position (sidesteps the _dock_y capture-timing trap; the preview tray never
animates). shop_panel.gd sets the WHOLE PreviewLayout subtree to mouse_filter
IGNORE ONCE in _ready — taps inert, drags fall through to App's swipe. This is the
load-bearing detail: App's swipe is _unhandled_input, so a full-rect STOP blocker
would have SWALLOWED the backdrop drag and broken swipe-from-shop; the persistent-
cell architecture (cells/rows built once, updated in place, never recreated) is
why the one-time inert sticks. That same inert subtree CLOSED the s42-logged
live-cell month-nav bug (off-rail taps no longer month-nav the clone).

SHOP RAIL ORIGIN MOVED TO THE HAMBURGER LINE — VERIFIED:
Rail top now at Layout.safe_top_inset()+24 (the hamburger's y), set in
shop_panel.gd _ready; .tscn baseline updated. FLAGGED not done (deferred): the
PreviewLayout itself doesn't take App's safe-area inset shift the real panels get,
so on a notched device the dupe content sits ~inset px high — one line when it
bites.

STICKER NAMING CONVENTION LOCKED + CATALOGUE ROWS ADDED — art renaming VERIFIED:
New stickers weren't auto-registering. Two DECOUPLED systems: ART (client) =
stickers.gd resolves res://assets/icons/<id>/<id>_sticker.png — the `_sticker`
suffix is MANDATORY, misses re-checked each call (no restart); CATALOGUE (server)
= the D1 `stickers` table, seeded in seed_devkit.sql, NOT touched by the DevKit
scenarios. CONVENTION (locked): snake_case underscore (not hyphen, not camelCase),
id BYTE-IDENTICAL in THREE places — folder name, file prefix <id>_sticker.png,
catalogue sticker_id. camelCase was the import-miss root cause (folder/file/
catalogue mismatch). water_bottle/button/cloud fixed + rendering; jesterHat/
shadowMan FOLDERS still need renaming to jester_hat/shadow_man (Morgan, in the
Godot dock so .import remaps). add_stickers.sql inserts four catalogue rows; these
fold into seed_devkit.sql so they survive a reset (Morgan). This resolves the
long-standing ACTIVE sticker-render path question.

GOTCHA (re-confirmed): `wrangler d1 execute --file=` failed with Authentication
error [code: 10000] on the /import endpoint (DB rolled back, NOTHING inserted)
despite valid auth — the known D1 import-endpoint token quirk. Fix: --command
(query path, sidesteps /import) or logout+login. Rows landed on the --command
re-run.

4.D SELECTOR RESKINNED TO THE SHOP-MIRROR RAIL — UNVERIFIED:
The leader/pool selector was a centred modal (functional stub). Rebuilt as the
RIGHT-edge mirror of the shop rail (sticker_selector.gd, full rewrite, code-built):
  - TOP DUPE: a copy of the leader parked over the real leader slot (its GLOBAL
    rect passed via setup — App now passes _leader_slot.get_global_rect()). It is
    the OPEN/CLOSE button (tap closes); the real slot is covered while open.
  - RAIL: translucent column below it (shop's stylebox), every OWNED sticker incl.
    the leader — the DUPE: the top button is spent on open/close, so the leader
    needs a rail presence for its context menu. In-pool = full opacity + ring,
    out-of-pool = dimmed, leader = a ★ badge.
  - TAP item -> one-entry context menu (the crown, popped to the item's left).
    LONG-PRESS item -> pool toggle. The crown SETS THE LEADER LIVE (the write
    exists; keeps pair_key rotation testable — the alternative was leaving leader-
    setting behind an unbuilt picker, a regression). ★ = "is leader", crown =
    "make leader" — distinct so the crown isn't overloaded.
  - No scrim; an invisible full-rect catcher gives tap-outside-closes. A context
    menu open absorbs its own tap-off (dismisses the menu only, not the selector).
Writes UNCHANGED from the old selector (set leader -> request_user_update
active_leader [rotates pair_key]; pool toggle -> active_stickers w/ never-empty
guard). App is the ONLY caller (grep-confirmed) so the setup() signature change is
safe. This is the precursor seam to 4.14b's full theme-manager context menu.

UNVERIFIED (device — reload first, class_name caching):
- The whole selector reskin: rail appears off the slot; top dupe + tap-outside
  close; long-press = pool toggle (never-empty holds); tap -> crown card -> sets
  leader (★ + dupe + slot repaint, crown greyed on the current leader); CONFIRM
  the dupe lands exactly over the real slot (if offset, the panel's centred in a
  wider viewport — the slot-rect wire is in place to fix it).

OPEN (carried):
- jesterHat/shadowMan folder renames; fold add_stickers.sql into seed_devkit.sql;
  the s41 shop endpoints into wire_regression_pack.md (still open).
- PROD deploy+reset to fold s41+s42(+s43) — dev-only; kills the dogfood pairing.
- WEEK-MODE DOC REWRITE still owed s38-s42. 4.FT accounting-seal hero beat open.
- The select->preview RENDER + 4.14b context menu wait on the retint pipeline.

NEXT: device-verify the selector reskin. Then the folds above; then prod deploy+
reset, or the week-mode doc rewrite (overdue). The retint pipeline (4.14b) unblocks
the shop preview render + the selector's eventual theme context menu.

Session 43 time: ~5h ESTIMATE (selector reskin UNVERIFIED — verification firms it).

Running total at session 43 close: ~218.75h
(s43 ~5h ESTIMATE. TWELVE time-debts now owed since s32 — the reconciliation pass
spans s32-s43 and is well overdue.)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION 44 — 2026-06-20 AWST — s43 verify + wire-pack fold (DEV)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECTOR RESKIN VERIFIED. The s43 4.D reskin passed all six device checks
(reload-first for the class_name caching footgun): right-edge rail off the slot;
top dupe + tap-outside close; long-press pool toggle w/ the never-empty hold;
tap->crown sets the leader live with ★/dupe/slot repaint; the dupe parks exactly
over the real slot (no viewport-centre offset). s43 thread 4 closed — its
UNVERIFIED banner clears.

WIRE PACK W21-W23 — s41 shop/seal endpoints folded in. Those endpoints were
wire-proven ad-hoc at s41 but never in the re-runnable pack; now added + dev-
PASSED on four-tasks-dev:
  - W21 fourth-tick freeze (completion_sticker frozen, sealed_at stays NULL —
    content seal is distinct from the accounting seal) + both 409 day_complete
    locks (write-path re-PUT; rest-path designation — the rest lock fires BEFORE
    the coin check, so it holds at the seeded 4,250).
  - W22 shop list (live catalogue minus owned).
  - W23 buy: sticker_not_found, insufficient_coins, the happy debit + owned/
    active append (4,250 -> 4,249), already_owned.
  Cosmetic fixed: the W23 coins-before read hit the wrong path on the composite
  user GET (coins nests under data.user there, not data.coins) — dropped from the
  pack; the buy response is the authoritative read.

OPEN (carried — FRESH-BRAIN, not for a tired session):
- Fold add_stickers.sql catalogue rows into seed_devkit.sql. PREREQUISITE for the
  prod deploy: the prod reset re-seeds, so the catalogue must be in the seed or
  prod's shop comes up empty.
- PROD deploy+reset to fold s41+s42+s43 — kills the live dogfood pairing
  (deliberate); then re-run the full wire pack against prod + re-verify the
  shop/swipe/selector there.
- jesterHat/shadowMan folder renames; week-mode doc rewrite (overdue s38);
  4.FT accounting-seal hero beat. Retint pipeline (4.14b/4.20) still gates the
  shop select->preview render + the selector's theme context menu.

NEXT: seed-fold, then prod deploy+reset (both fresh-brain). Coin sprite swap
(tile 4.A, swap _make_coin in CoinFX.gd) is a separate small job — new chat.

Session 44 time: ~1h ESTIMATE (verify + wire-pack fold + bookkeeping).

Running total at session 44 close: ~219.75h
(s44 ~1h ESTIMATE. THIRTEEN time-debts now owed since s32 — reconciliation still
pending.)