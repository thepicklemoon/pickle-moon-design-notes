FOUR TASKS — Godot Devlog (v3)
==============================
Last edit: 2026-06-12 AWST (session 33, laptop)
Last updated: 2026-06-12 (session 33 — 4.26 notifications BUILT + D-N1-4 device-green (ambient cases open); 4.25 SERVER HALF shipped + full wire pass on dev (W13r/W14r/W19/W20 + W11/W12 re-proven); gradle build enabled; W18 + DevKit.gd rename cleared.)
Engine: Godot 4.6
Studio: THE PICKLE MOON
Target stores: Google Play, Apple App Store

This is devlog v3. Prior files are archive-only — read them for
historical reconstruction of HOW a decision was reached, not for the
decision itself:
  - `four_tasks_godot_devlog_v1_sessions_1-9.txt`   — sessions 1-9
  - `four_tasks_godot_devlog_v2_sessions_10-25.txt` — sessions 10-25

Design docs (in `four-tasks/` under `pickle-moon-design-notes`) are
the authority for everything locked. The summary below is current
state at session 25 close; session 26+ append chronologically.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AI READING GUIDE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Read this devlog before any code. The summary block is the entry
point; sessions append after it. Design docs carry the detail and
the authority — the devlog/todo are lean pointers, not archives.

Key locked design docs (authority):
  - architectural_preference          — clarity over cleverness (meta-rule)
  - pair_key                          — three-identifier model (user_id /
                                        pair_id / pair_key)
  - partner_reactions                 — field write rules (DEFERRED to v1.x)
  - staggered_disclosure              — day-2 reveals, gesture dismissal
  - morning_sequence                  — 12-beat ceremony + post-sequence
                                        presentation queue (collision resolved
                                        session 26)
  - economy_redesign                  — coin/streak/rescue AUTHORITY (any
                                        number or multiplier build reads it)
  - recovery / timezone_and_sealing / motd / stamp_tier / theme / week_mode /
    rate_limiting / tracking / onboarding / content_authoring_tool
  - wire_regression_pack.md           — re-runnable server wire tests (s32)

Repo map: `pickle_moon_repo_index.md` (raw-URL resolver). Always use
`refs/heads/main/` raw-URL form.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SUMMARY — STATE AT SESSION 25 CLOSE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ARCHITECTURE (locked):
  - Server is source of truth: Cloudflare Worker + D1 (Oceania), TS.
    Three-worker target dev/staging/prod. Godot client mirrors, polls,
    optimistic local writes w/ rollback.
  - Identity: user_id (person, forever) / pair_id (relationship) /
    pair_key (recovery hash). Self-writes only for v1.0; partner
    reactions deferred to v1.x.
  - HTTP envelope {ok,data}|{ok,error,message}; codes 200/400/404/409/500
    (+429 rate-limit only). No 401/403 (perm-denied folds to 404).
  - Sealing: LAZY walkback. Days sit unsealed; the morning claim
    (POST /users/:id/claim) walks back, seals the most recent unsealed
    accounted-for day, computes stamp/coins/streak then. `morning_payout_
    due_for` column dropped as redundant. (Eager-seal-at-completion
    considered + REJECTED session 26 — unnecessary over-engineering;
    lazy is sufficient. 4.25 span-sealing DOC-LOCKED s32: the walk will
    seal the whole span back to the last seal, not one target.)
  - File naming: autoloads PascalCase (Backend/State/Identity); else
    snake_case; internal code always snake_case.

PHASES:
  - Phase 1 (backend), Phase 2 (calendar/tray/swipe/poll), Phase 3
    (identity/onboarding/pairing/recovery) — CLOSED.
  - Phase 4 = ALL-CODE skeleton (build now, stub art). Phase 4b = painting
    only (4.A-4.F). Phase 5 = store submission.

SHIPPED THROUGH SESSION 25:
  - Full calendar + tray + two-panel swipe + poll-merge. Task list,
    bonus pill, streak bar. Onboarding (path choice, name, avatar,
    companions shell, MOTD placeholder, tasks). Recovery (3.10B)
    prod-verified end-to-end. PHASE 3 CLOSED.
  - 4.1 glitch typewriter (GlitchType.gd autoload + glitch_reveal.gd
    worker). MOTD spinner widget built standalone (msg_picker/msg_wheel).
    Companions screen (5b shell) — commits leader + 5 to active_stickers.
  - DevKit: always-on overlay, dev worker (four-tasks-api-dev) + dev D1,
    OS.has_feature("devkit") flag. Scenarios reboot through Loader.
  - Server: /resolve_solo, /motd/pool, /motd/roll, active_stickers
    persistence fix — all deployed dev+prod.

KEY TERMS (do not collide):
  - STICKER = calendar cell completion mark (active_leader art). Fires as
    its OWN hero moment when the 4th task is ticked — NOT part of the
    morning sequence.
  - STAMP = task-tray sealed-day artefact (tier-coloured + message).
    Belongs to the morning sequence (beat 7) and persists in the tray.
  - active_leader = identity-hashed visual sticker (was emoji/icon).
  - MOTD = the day's message; lives in header + that day's tray subtitle.
    Stamp message ≠ MOTD (two strings, two pools).

CARRIED NOTES / OWED:
  - Hand-written scene UIDs on newer scenes — delete .godot/ if a panel
    mis-renders on fresh import.
  - msg_wheel.gd integer-division warnings + dead var — cosmetic cleanup.
  - Spinner tuning deferred (column widths, font, doubled selector band,
    EMOJI REEL renders sticker IDs as TEXT not art — wheel is Label-based,
    needs a TextureRect column).
  - Dev D1 carries test cruft (TestUser/TestPartner + stale rows) — not
    cleaned, not blocking. Prod wiped to zero post-recovery-testing.

Running total at session 25 close: ~121.75h.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION 26 — 2026-06-05
4.4 CLOSED · beat-collision LOCKED · 4.6 coordinator built (1 beat left)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4.4 MOTD SPINNER — CLOSED (deployed dev + device-verified). Time: ~2h.
  - onboarding_motd.gd/.tscn (NEW) — screen-6 panel hosting msg_picker;
    captures the just-picked sticker pool, auto-spins, "spin again" reroll,
    Continue emits composed_message(). Economy-fenced.
  - msg_picker: open(emoji_override) for onboarding injection; reroll()
    (snappy) split from spin() (dramatic). msg_wheel: spin_to() velocity
    wrap-scroll vs jiggle_to() damped reroll wobble.
  - onboarding spine reordered: THEME_THEATRE moved AFTER tasks (gather
    leader+companions+MOTD+tasks, THEN commit). create_user now sends
    `today` (client ISO) + `motd`; server writes users row AND day-0 row
    (accounted_for=1) in one batch.

BEAT-COLLISION DESIGN — LOCKED (folded into morning_sequence doc).
  - "Collision" = content drops / patch notes / popups competing at boot,
    NOT two-days-sealing (walkback handles that). Resolution: the morning
    sequence is SOVEREIGN — always first, uninterruptible, NOT a queue
    member but the gate everything waits behind. A simple presentation
    queue drains AFTER the sequence cedes control, once per app-open.
    No replay of a completed sequence. Unblocked 4.6.

4.6 MORNING SEQUENCE COORDINATOR — built, device-verified. Time: ~5h.
  Spine + 11 of 12 beats real; beat 6 (coin payout) the only stub.
  - morning_sequence.gd (NEW) — driver Node. App owns it, hands refs
    (self tray, header subtitle, picker, user_id, local_date) via run().
    started()/finished() boundaries; idempotent single cleanup; abort().
    Drives claim via State; beats await each other.
  - State.gd — claim handler split (A2): merges banked user silently +
    writes the sealed day onto State.days SILENTLY (no signal, so the
    calendar doesn't repaint mid-ceremony) + emits new morning_claimed
    (sealed_day) signal INSTEAD of the old eager load_user reload (which
    spoiled the reveal). refresh_after_morning() repaints at beat 12.
    _apply_seal_to_day() stamps stamp+sealed_at onto the row.
  - App.gd — owns coordinator + picker overlay (z100) + full-screen input
    blocker (z200, mouse_filter STOP, added last). Fires the sequence once
    on first self-load. started → blocker up + swipe off; finished →
    blocker down + swipe on. Blocker swallows ALL user input (cells, month
    nav, tray close, swipe, DEV btn) — device-confirmed dead.
  - tasks_tray.gd — NEW capabilities driven by the coordinator:
      · lift_to_centre()/return_to_dock() (beat 5/8) — fly the card to
        viewport centre, full-screen dim behind, restore exact dock.
      · wobble() (beat 4) — damped POSITIONAL overshoot in the slide-out
        direction (telegraphs the lift; wheel-jiggle family).
      · STAMP = data-driven render. ONE path: _render_stamp_from_data()
        draws the stamp if the open day is sealed (message from days.stamp,
        tier DERIVED from tasks_done count — shared w/ cell, can't drift).
        Called on every open → permanent, shows on partner's open of your
        sealed days too. slap_stamp() (beat 7, no args) renders-then-
        animates the SAME thing. NOT freed on return — persists via data.
        Placeholder art (tinted panel + message); real art is 4.C.
  - DevKit harness: index.ts refactored default seed into shared
    seedDefaultSpread(env, mode); added scenarios morning_payout
    ("unsealed_worked" → seals orange) + morning_rest ("unsealed_rest" →
    purple) via insertUnsealedDaySql (sealed_at NULL). TestUser/TestPartner
    now seed active_stickers + real sticker leaders so the emoji reel has
    art. devkit.gd saves TestUser identity post-seed for the morning
    scenarios (was the "nothing happened" bug). devkit_menu rows flipped
    live. NEEDS REDEPLOY done.

  VERIFIED on device: open yesterday → wobble → lift to centre + dim →
  (stub coin hold) → stamp slaps + lingers → return + close → picker spins
  → MOTD glitches into header → today opens → calendar refreshes to sealed
  state ONLY after (reveal not spoiled) → control returns, all input dead
  throughout. Stamp PERSISTS — reopening yesterday shows it from data.

  STUB LEFT: beat 6 — task payout + coin fly. The last beat; its home is
  CoinFX (tile 4.3, no home until now). Per-row jiggle (700ms stagger) +
  coin fly + count-up (worked) / purple strike (rest). Currently a hold.

DECISIONS:
  - Eager-sealing (seal at 4th-tick/rest-apply) considered + REJECTED.
    Solving a non-problem; lazy walkback is sufficient. Noted so it isn't
    re-litigated.
  - Stamp is data-driven (stored on day row, rendered from it everywhere,
    slap is cosmetic) — not a ceremony-owned node. Single source of truth.
  - Tier always DERIVED from tasks_done count, never stored — can't drift
    from the cell colour.

OWED (new):
  - Cross-month seal nav — if the sealed day is in a prev month the calendar
    must nav there before beat 3. Not built (same-month opens directly).
    Owed with tray work.
  - Tray-MOTD subtitle (the "diary" beat, system map §7.5 fold-in) — needs
    a subtitle Label on TasksTray + the claim/row to carry the day's motd.
    Stubbed at zero hold in the coordinator.
  - Beat 4 wobble direction + lift fade-out speed = in-context tuning once
    the middle is full.

Session 26 time: ~9h.
Running total: ~130.75h.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION 27 — 2026-06-08
4.3 + 4.6 CLOSED · economy LOCKED · ceremony-gate refactor
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ECONOMY REDESIGN — LOCKED. New doc four_tasks_economy_redesign_notes.md
is now AUTHORITY; supersedes the monetisation doc's coin-tuning +
pair-pricing + pair-shared-coins prose (those described the prototype,
not the shipped per-user schema). Locked:
  - Per-user everything: coins, streak, library all individual. Only
    library ACCESS crosses the pair (tier-1 sub unlock). No shared
    wallet / pack list / co-ownership.
  - Per-task 900-1400 base. Partner multiplier 1.2/1.3/1.4/1.5 at 1-4
    partner tasks. Streak bonus +1%/day perpetual, TWO-SUB-ONLY.
  - Sub tiers: tier-1 (either) = library only, zero economy. tier-2
    (both) = streak bonus + x1.5 partner bonus (2.25x cap).
  - Solo subbed = phantom 1.5x, no streak (can't reach two-sub).
  - Rest 50,000. Stickers 20k-100k by UI volume (300-600 killed).
  - Cadence: free == one-sub (~7d); only two-sub accelerates. Doc's
    old cadence table DEAD.
  - OPEN TENSION logged: perpetual streak vs fixed sink (contained to
    two-sub cohort + self-limits on streak reset). Cap option noted,
    not applied.
  - Claim handler currently naive (per-task sum only) — doc is the
    TARGET; multiplier/sub/streak application owed in the handler.

4.3 CoinFX — CLOSED. New CoinFX.gd autoload (REGISTER as autoload;
filename PascalCase for the Android case-sensitive PCK). fly_coins
(rect->target, staggered, shrink+fade), count_up (display-only, never
touches State), wiggle (taskWiggle port), coins_for_row (4-7). Stub
coin art = yellow Label; real sprite is 4.A (swap _make_coin only).

4.6 MORNING SEQUENCE — CLOSED. Beat 6 real (last stub gone):
  - Per-row cascade (700ms): done -> wiggle + coin fly + count-up tick;
    undone -> wiggle + reveal MISSED; rest -> wiggle + reveal purple.
    coins_earned split evenly across done rows (cosmetic; total real).
  - Count-up architecture: claim merges final total into State BEFORE
    the ceremony, so beat 6 rewinds the display to (total - earned) and
    climbs back. Display-only via streak_bar.set_coin_display + CoinFX.
  - Cross-month nav (beat 3) real: calendar.show_month() before the tray
    opens if the sealed day is a prior month; beat 11 navs back to today.
  - Picker-invisible bug FIXED: open() never set visible; beat 9 now
    shows the picker before spin, hides after settle.
  - tasks_tray: list forwarders set_task_pre_payout / task_rows /
    reveal_task_row. task_list: pre-payout render mode + reveal_row +
    get_rows; suppresses its own auto-render while pre-payout.
  - msg_picker: card + selector band added (was bare reels over the
    calendar), styled at runtime from Palette (PICKER_* consts) — NOT
    baked in scene, so the theme system reskins it later. msg_wheel
    words given a dark font colour (were white-on-white, invisible once
    the card went in).

CEREMONY-GATE REFACTOR. Collapsed the per-surface mid-ceremony repaint
guards into ONE State.ceremony_active flag + ceremony_changed signal.
App flips it on started/finished; reactive surfaces (streak_bar,
panel_heading) read it in their render guard and settle on release. New
reactive surface needs zero App change. Two distinct suppression
mechanisms now documented in State: (1) source-side emit suppression
(silent seal merge — calendar) for single consumers; (2) this surface-side
gate for consumers whose trigger must still fire for others (Poll, the
morning trigger). task_list._pre_payout stays separate (render-MODE,
finer than the gate). Tray clear_stamp / direct-drive unchanged.

BUGS FIXED (audit pass): streak_bar coin-flash spoiler (claim self_changed
repainted final total before the count-up); panel_heading subtitle vs
beat-10 glitch clobber; streak_bar was reading current_streak (always 0)
— now streak; coordinator nav + GlitchType-null hardening; msg_wheel
white-on-white text on the new card.

OWED (carried): claim handler economy application (multiplier/sub/streak);
tray-MOTD diary subtitle (still zero-hold stub); in-context tuning (wobble
dir, coin size band, picker card width [full-viewport now], picker entrance
anim, MOTD font); cross-month verified-on-a-real-1st only.

Session 27 time: ~8h.
Running total: ~138.75h.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION 28 — 2026-06-09
claim economy LIVE · partner-rest fix · bonus-line + diary-MOTD beats
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CLAIM HANDLER ECONOMY — LIVE (index.ts). Was naive per-task sum; now
applies the economy doc in full at seal:
  - base (per-task 900-1400 sum) × partner multiplier (1.2/1.3/1.4/1.5
    by partner's task count ON THE SEALED DATE; explicit lookup table,
    not 1.1+n*0.1 float-drift). ×1.5 again if two-sub (2.25 cap).
  - Two-sub-only streak bonus: round(base*mult * streak*0.01), PRE-claim
    streak (the day being sealed doesn't bump its own bonus).
  - Solo: 1.0, or phantom 1.5 if subscribed-solo. No streak (can't reach
    two-sub).
  - Partner/sub reads gated behind base>0 — rest/grey skip them (0 base,
    mult moot). Partner row via the existing pair_id pattern; partner's
    sealed-date day read for completion; both subs read for two-sub.
  - Baked into coins/lifetime_coins immutably (no recompute on later
    lapse). Verified on-device: 2-task day × partner-did-3 = ×1.4, deltas
    ~3210/3259 (correct against the ~3220 midpoint).

PARTNER-REST → ×1.5 FIX. First cut read rest as 0 tasks → ×1.0,
contradicting the LOCKED rule (morning_seq doc line 154) AND the client
pill (Bonus.gd REST_DAY_MULTIPLIER). Corrected: partner query reads
rest_day; partnerRested forces 1.5; partnerContributed (rest OR ≥1 task)
gates the two-sub boost so rest behaves exactly as a 4-task day (2.25
under two-sub). Pill was right all along; server was wrong.

CLAIM RESPONSE gains sealed_day.multiplier (applied factor, for the
ceremony bonus line) + sealed_day.motd (the sealed day's own MOTD, for
the diary glitch). targetRow already SELECTed motd; just surfaced.

TRAY SUBTITLE SLOT REPURPOSED (tasks_tray.gd, NO scene change). The
existing LabelLabel ("TASKS FOR" eyebrow) is now state-driven on open:
sealed day with a MOTD → that day's frozen MOTD; else → "TASKS FOR".
_pre_payout flag (mirrors task-list pre-payout) keeps it neutral during
the ceremony so the coordinator fills it. subtitle_label() exposed for
the coordinator to glitch into (same pattern as the header at beat 10).
One slot, three registers distinguished by casing (all-caps eyebrow /
Title-case bonus / natural-case MOTD) — one style. Sealed-day reopen now
shows that day's MOTD in perpetuity (immutable, frozen by sealed_at).

MORNING SEQUENCE BEATS (morning_sequence.gd) — last stubs gone:
  - Beat 5a (NEW, after lift): glitches "Coin Bonus = ×1.4" (from the
    claim's multiplier — authoritative, NOT the live pill which reads the
    wrong day at morning-open) / "Rest Day" on a rest claim. Holds through
    the payout.
  - Beat 7 (_beat_tray_motd_subtitle, was zero-hold stub): the sealed
    day's own MOTD glitches into the SAME slot as the stamp lands,
    overwriting the 5a line (GlitchType auto-cancels the prior reveal on
    the same label — clean overwrite, no glitch-out pass).
  - Beat order: lift → 5a → payout → stamp+MOTD → return/close → picker →
    header MOTD → today's tray → refresh. The new MOTD spun at beat 9
    (header, beat 10) is distinct from the diary MOTD at beat 7 (tray) —
    two strings, two surfaces.

DEVKIT SEED FIX. morning_payout/morning_rest seeded the claimable day
with an empty motd → beat 7 had nothing to render (the "MOTD isn't
showing" symptom — code was correct, seed was bare). Now seeds
"showed up anyway" (worked) / "took it easy" (rest), standing in for the
MOTD that day's own sequence would have set.

DESIGN DOC. four_tasks_morning_sequence_design_notes.md updated (beats
5a/7, the bonus-line rationale, partner-rest → 1.5 as a server
obligation, claim response multiplier+motd fields, the subtitle dual-use,
rest-day 5a shows "Rest Day"). Signed off before build.

VERIFIED on-device: worked ceremony (bonus line → coins → MOTD
overwrite), rest ceremony ("Rest Day" → MOTD overwrite), perpetuity
(reopened sealed day shows its MOTD). Partner-rest payout ×1.5 confirmed.

4.8 SAME-DAY REST SYSTEM — mechanism complete (content + device test
deferred). Long-press today's own cell to toggle a rest day.
  - Server: POST /users/:id/days/:date/rest. Body {rest_day, local_date}.
    Same-day only (path date must == local_date, else 400 not_same_day;
    past days are the parked escape hatch). ON debits REST_COST (50,000;
    409 insufficient_coins below it) + sets rest_day=1 + accounted_for=1.
    OFF refunds + rest_day=0 (accounted_for stays — monotonic). Idempotent
    (re-toggle same state = no coin move). Debit+flag in one D1 batch
    (transactional). Returns {user, day}. Charge lives at DESIGNATION, not
    claim — claim handler untouched, economy-doc "paid at designation"
    stays accurate. WIRE-TESTED 5/5 (on/off/idempotent/insufficient/
    not-same-day).
  - Client: rest_confirm.gd (new, code-built modal — scrim + centred
    card, "Take the day off?" copy, yes/no; below 50k shows shortfall
    copy + disables yes). App connects cell_long_pressed (SELF calendar
    only), gates to today, opens dialog on toggle-on / silent-refund on
    toggle-off. Backend.set_rest_day + rest_day_set/_failed signals.
    State.toggle_rest_day rewired to the cost endpoint: optimistic flag
    flip, snapshot rollback, coins taken from server response (never
    guessed). Cell + tray rows repaint to rest-purple FOR FREE via the
    existing day_changed plumbing (calendar_grid + task_list already
    classify rest_day → purple/locked; is_toggleable already excludes
    rest) — no calendar/tray/cell changes needed.
  - Coin tick: streak_bar.animate_coin_display(from,to) — self-contained
    number tween (no CoinFX, no flying coins), direction-agnostic (down
    on spend, up on refund). App drives it via a self_changed one-shot
    capturing pre/post balance.
  - Dialog centering bug fixed mid-build: mixing anchor PRESET + manual
    position pinned it top-left; now explicit anchors + offsets throughout.

CLOSED (both carried from S27 OWED): claim handler economy application;
tray-MOTD diary subtitle.

OWED (carried): in-context tuning (wobble dir, coin size band, picker
card width, picker entrance anim, MOTD font, beat-5a hold 0.45s,
rest-coin-tick 0.45s); cross-month verified-on-a-real-1st only;
RevenueCat sub plumbing sets subscription_active (Phase 5 — the economy
reads it, nothing writes it yet).

OWED (4.8 rest, before tile close): device-test the full long-press loop
(finger feel, dialog at phone size, tick); rest-day task-label content
swap (~15-30 phrases drawn 4/rest-day — rows still show real task labels
on a rest day; DEFERRED, felt cheesy). No rest-day design doc (spec
scattered across economy/morning-seq/system-map, deemed adequate).

Session 28 time: ~4h.
Running total: ~142.75h.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION 29 — 2026-06-10
Full-stack bug sweep · 20 fixes · forecast + todo restructure · doc sync
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERIFIED: wire W1-W9 passed 2026-06-11; device D1-D11 passed session 31.

BUG SWEEP — full stack against the locked docs: index.ts (all 16
routes), schema.sql, wrangler.toml, every autoload + UI script. 27
graded findings. Clean bills worth recording: walkback `<` invariant,
both ceremony-suppression mechanisms, poll selective merge, glitch
worker lifecycle, derived stamp tiers, calendar math (incl. negative
month deltas + leap years), envelope/status discipline on all routes.

FIXES SHIPPED:
  #1  CRITICAL App.gd — claim trigger per-DAY not per-process.
      _morning_triggered (session bool) → _morning_last_claim_date;
      APPLICATION_FOCUS_IN re-triggers on a new local date (Poll's
      proven mechanism); Backend.claim_failed re-arms so a failed
      claim retries on next focus-in (previously never retried).
      Android recents-resume silently stopped the daily loop —
      no claim, no seal, no payout. DevKit always reboots through
      Loader, which is why device testing never caught it.
  #2  CRITICAL index.ts — writeDay upsert sets accounted_for=1 on
      insert AND conflict. The doc's "ticking a task, saving a MOTD"
      engagement triggers were unimplemented; combined with #1, a day
      worked across midnight-in-session was PERMANENTLY unsealable.
      The claim's false "Sole writer of accounted_for" comment fixed.
  #3  HIGH index.ts — writeDay now 400s rest_day (was a silent free
      rest day bypassing the 50k charge). Strict-not-silent per the
      locked write-rules pattern; rest endpoint is the sole writer.
  #4  HIGH morning_sequence.gd — abort() DELETED. It could not stop a
      suspended coroutine (zombie resumes after "aborting"); nothing
      called it; Android freezes the loop on background anyway, so
      freeze/resume is the correct model for free. INTERRUPTION
      header rewritten; design doc synced this session.
  #5  HIGH State.gd — refresh_after_morning() routed through
      poll_user (selective merge preserves the in-flight beat-10
      set_motd write). load_user's full reset could stomp the
      just-committed MOTD at settle; the coordinator comment claimed
      a protection that did not exist.
  #6  Backend.gd — 15s HTTPRequest timeout (default = none; a stalled
      boot claim meant no ceremony, ever, with no failure signal).
  #7  State.gd — rollback snapshots keyed per-operation (day|/rest|
      namespaces). The single shared slot let any completing task-tap
      wipe an in-flight rest toggle's rollback. Residual documented:
      day_written responses carry no request identity, so the shared
      flight FLAG can still clear a beat early (cosmetic only).
  #8  index.ts — identity length caps enforced (name 49 / username 40
      / active_leader 50, post-normalisation, pair-key doc §12) with
      the doc-mandated value_too_long error code; all write paths
      covered incl. the rotation route.
  #9  index.ts — unpair notice RECIPIENT-only per tile 4.21; the
      initiator's notice is NULLed (also clears stale notices from a
      prior relationship).
  #10 rest cost server-shipped — new `economy` container on GET
      /users ({rest_cost}); State.rest_cost() with the sole permitted
      client fallback; rest_confirm's duplicated constant deleted.
      Economy retunes are now one server constant.
  #11 App.gd — rest coin-anim ONE_SHOT tracked (_pending_rest_anim)
      and torn down on rest_day_set_failed; State.toggle_rest_day →
      bool so the anim only arms on a confirmed dispatch. (Phantom
      count-up on the next poll was the failure mode.)
  #12 index.ts — claim coin writes RELATIVE (coins = coins + ?),
      matching the rest endpoint, so the two coin writers commute;
      the read-modify-write window could swallow a concurrent rest
      debit. Streak/longest stay absolute (no concurrent writer).
  #13 tasks_tray.gd — open/close tween to ABSOLUTE dock targets
      (_dock_y captured at first open). Relative offsets + callback
      resets meant interrupted closes baked drift in permanently and
      poisoned the ceremony lift's baseline.
  Low tier: wrangler.toml INVERTED (top level mirrors DEV — bare
  `wrangler deploy` lands on dev; prod requires --env production;
  the old "accidental deploy goes to prod is safe" comment was
  backwards risk thinking); Poll.gd debug prints stripped;
  insertDaySql's stale "State.gd hardcodes UTC+8" comment corrected
  (false since s23); header MOTD fallback walks back to the most
  recent prior MOTD (yesterday-only broke the never-empty invariant
  after one skipped day); lift dim sized from get_viewport_rect()
  (keep_height varies logical width by aspect); fetch_motd_pool
  concurrent-call coalescing (duplicate-connect error + double
  fetch); bug_report cap 500 → 2000 per the write-rules doc.

PARKED FINDINGS (recorded in todo): yesterday_iso epoch−86400 DST
edge (IANA v1.x family); rotation/join TOCTOU returns 500 not 409 on
a simultaneous race (pair_key UNIQUE keeps correctness — note only);
bonus pill never shows the two-sub ×1.5 (lands with 5.s paywall);
partner payload hands the partner your user_id = write credential
(recorded as a decision in pair-key §9.1a, written this session);
tile 4.14c appears ALREADY DONE (is_interactive blocks DEAD_FUTURE
taps + long-presses) — device-verify and tick.

FORECAST REVIEW → TODO RESTRUCTURED (full rewrite shipped):
  - Remaining Phase-4 code ≈ 90-105h ≈ ~3 roster breaks to
    code-complete; realistic store launch Sept-Oct 2026.
  - Week mode STAYS v1.0 (Morgan's call, against deferral pressure).
    Interaction fork documented in the week-mode doc: locked
    two-gesture model vs Morgan's popup-with-radio-selectors
    description — resolve at design sign-off before 4.D2.
  - Streak-semantics PARKED item resolved MOOT (week mode is label
    overrides only; every day still has four tasks) → 4.9 unblocked.
  - Leaderboard KILLED for v1.0; eternal-stats optionality preserved
    (lifetime_coins/longest_streak live; tracking doc deferred).
  - Content-drops delivery promoted to tile 4.22 — the shipped
    client's pack-delivery path is the enabler of the post-launch
    "endlessly paint stickers" loop and was buried as a PARKED line.
  - 5.10 Play Console setup pulled forward (verification lead time;
    IAP products must exist before RevenueCat is testable); 5.s
    subscription plumbing tiled out of PARKED.
  - Rest-purchase ESCAPE HATCH endorsed for v1.0 design (coin sink
    inside the open-app flow; welcomed because it protects the
    growing streak bonus). Conflation with the seal-day HERO BEAT
    flagged — two distinct items, one combined design conversation
    (same ceremony surface).
  - Android-first considered, NOT taken (store-spend at launch +
    iOS adopters), kept as a conscious lever (tile 5.0).

DOC SYNC (five full files shipped): morning_sequence (freeze/resume
interruption model, per-local-day trigger semantics, soft walkback
invariant, accounted_for triggers marked implemented); system_map
(§5.3/§9.2/§9.4/§10.x rewritten to as-built s29; honest header
currency note — §6-7 component inventory refresh still owed);
week_mode (fork section + moot resolution + scope reconfirm);
pair_key (§9.1a intra-pair credential decision); stamp_tier (grey
tier row + pool key + stamp-lives-on-tray terminology fix).

SECOND PASS — AUTONOMOUS RUN (same day, Morgan on-roster, explicit
go-ahead to build without per-step device verification; scope line:
server-side TypeScript, code-built dialogs on the rest_confirm
precedent, DevKit tooling, docs/content — NOTHING ceremony-adjacent,
nothing design-gated):

  DEVKIT MOCK CLOCK [~, test D10] — State._mock_today_iso: feature-
  gated setter (release builds no-op), today_iso/yesterday_iso mock-
  aware, shift_iso_days pure-calendar arithmetic (no epoch, no DST),
  cleared by clear(); flows through the claim's local_date so a mocked
  tomorrow exercises the REAL seal path. DevKit menu "Mock clock"
  section: status / Today +1 / Clear. Date-boundary testing no longer
  needs Android settings.

  4.22 SERVER RECEIVE-SIDE [~, tests W9/W10] — schema: content_drops /
  content_drop_assets / content_drop_deliveries (comm_templates
  deliberately publishing-stage). GET /users/:id/drops (server: live +
  forward-only + undelivered; client: release-day local-time crossing,
  consistent with client-owns-the-clock; subscriber_only returned NOT
  filtered — sub-pack announcements are conversion pull). POST
  delivered ack (explicit, idempotent, never implicit on read — the
  decoupling principle). scheduled() Thursday cron promotes next
  queued drop; wrangler [triggers] on all three configs. SURFACED:
  runtime sticker-asset loading (client fetch/cache of R2 PNGs) is
  UNDESIGNED — joined to the theme-prep conversation; until it lands,
  drops deliver announcements + catalogue data, not new art.

  4.5 SERVER HALF [~, test W8] — POST /users/:id/motd/reroll: relative
  debit, 409 insufficient_coins, returns updated user + fresh roll.
  REROLL_COST=100 placeholder via the economy payload ({rest_cost,
  reroll_cost}). DECISION recorded at the constant: the fee is vent-
  pricing on the legitimate flow, not resource protection — words are
  free via /motd/roll, MOTD writes free via writeDay; bypass is
  self-spoiling only. Client half (picker lock + confirm) remains.

  4.12 MECHANISM [~, test D11] — bug_catcher.gd: code-built,
  self-contained compose→sending→thanks/failed against the live
  endpoint; 2000-cap live counter; ONE_SHOT teardown on every exit
  path (the rest-anim leak family). Trigger hosted in DevKit →
  Dialogs; real trigger is one line when 4.11's help menu lands;
  confetti waits for 4.9's SHARED system. Caught in self-review:
  two invented Palette constants → TEXT_HEADER/TEXT_WEEKDAY.

  DRAFTS — terms_of_service_draft.md (5.9a [~]: plain-language, ACL
  carve-out, store-billing refunds, pair-visibility honesty; needs
  Morgan review + hosting). four_tasks_promo_codes_design_notes.md
  (formalises s12 schema decisions: one-code-per-user-ever, permanent
  founders rate, attempt logging incl. failures, codes never grant
  coins, flag-vs-rate distinction).

  SKIPPED deliberately: 4.9 (modifies the claim handler — the most-
  changed unverified file), 4.14a/b (fork point + design-gated),
  4.D2 (interaction fork is Morgan's call), all ceremony surfaces.

Session 29 time: ~5h.
Running total: ~147.75h.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION 30 — 2026-06-11
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Mobile design session (Morgan on roster) → same-day laptop wire pass.
Client device-verified session 31 (D12-D15 pass). Time: ~5h.

WEEK MODE FORK RESOLVED → MODEL B (popup editor). Long-press the tray
heading's day name on ANY open day → popup, four fields + per-slot radios.
Radio ON = override stored on selection; OFF = slot tracks the standard four,
authored text dormant per-slot. Per-weekday toggle DIES (users.week_mode_
weekdays retired-unused); override table reshapes at 4.D2 (per-slot text +
active_slots bitmask; explicit drop-and-recreate — IF NOT EXISTS won't).
Model A's epitaph + the seventh discovery-arc iteration recorded in the doc.

STREAK RULE AMENDED (economy doc is authority): ≥1 task ADVANCES the streak,
rest holds, ONLY zero-task days break — sealed grey or never-opened gap.
Rationale: no paternalising; the bare minimum keeps the chain alive. Found
in the live handler: gaps NEVER broke streaks as shipped (unsealed days never
ran streak math) — closed via date-adjacency vs last sealed day (derived, no
new column). Gap break zeroes the streak BEFORE the sealing day counts AND
zeroes that payout's streak bonus.

STREAK RESCUE GATE (the escape hatch, designed + built). All forks Morgan's:
zero-task breakers only; RESCUE_COST = REST_COST = 50k (own constant,
economy.rescue_cost shipped); NO frequency cap (price-only guard — econ-doc
open-tension #2 rewritten: containment materially weakened, accepted twice);
offer only when streak ≥ 1 AND exactly ONE purchasable day saves it (2+ day
gaps are dead — no false hope for sale). Shape: claim returns pending_rescue
instead of sealing; POST /users/:id/claim/resolve re-derives everything
(re-entrant) — decline IS sealing grey, accept IS convert/create-rest + seal,
both atomic, so the seal itself enforces one-shot (no tracked state; app-death
mid-offer self-heals via re-offer). Beat 0, PRE-ceremony, over the flat
calendar — the lockout stays acknowledgement-not-action. index.ts refactor:
single sealTarget() shared by claim + resolve (one economy implementation,
two doors); deriveTier/evaluateRescue/lastSealedDateBefore helpers; devkit
scenarios morning_grey + morning_gap. WIRE-VERIFIED same day: W11-W17 all
pass (incl. atomic-batch proof — rescue day + target share one sealed_at).

CLIENT HALF (device-verified s31, D12-D14): rescue_confirm.gd — rest_confirm pattern
with two deliberate deviations: scrim does NOT cancel (declining is
irreversible) and every exit is an answer. State routes pending_rescue to a
new signal; the ceremony coordinator's parked one-shot morning_claimed picks
up the resolve's claim-shaped response — morning_sequence.gd UNCHANGED, as
designed. Dialog mounts above the input blocker (later in tree + z 300).
Resolve failure re-arms the per-day claim marker (self-heal).

SEAL-DAY HERO BEAT (designed + built, device-verified s31, D15). Evening celebration:
4th tick (or rest confirm) → cell PROXY flies up + enlarges (real cell never
reparents), sticker pops (placeholder glyph until 4.D), card frames payout
as arriving tomorrow. NO AMOUNT — the morning keeps the reveal, which
dissolved the s29 "reshapes ceremony" worry: zero ceremony/server changes.
Once per local date (Prefs.hero_shown_date), day-1, rest variant delays
~0.8s for the coin tick-down. hero_beat.gd self-contained; App owns trigger
policy off day_changed; 2 DevKit Dialogs rows bypass the gates for tuning.

ALSO: s29 wire tests W1-W9 PASSED (this date, prior chat) + sweep-chat patch
on getEligibleDrops (per-drop parse isolation) — index.ts s30 build carries
it. New docs: four_tasks_hero_beat_design_notes.md, session_30_wire_tests.md.
Amended: economy, morning-sequence, week-mode docs. (Sync remainders live in the todo bookkeeping block.)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION 31 — 2026-06-11
DEVICE LEDGER CLEARED (D1-D15 all pass) · 5 fixes shipped + verified same day
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LEDGER: D1-D15 ALL PASS — s29 sweep regressions + s30 rescue/hero closed.
New W18 (coin bound, below) UNVERIFIED; W10 cron still optional-open.

FIX devkit.gd → DevKit.gd rename. Autoload path case mismatch: Windows
editor resolves case-insensitively, Android PCK doesn't → autoload Nil on
device, menu dead. Convention says autoloads are PascalCase; the file was
the one violation.

FIX ceremony stage normalization (found by D2). show_for_date is a TOGGLE:
tray already open ON the seal date made beat 3's awaited `opened` never
fire → coordinator deadlock, input blocker stuck forever. Beat 0b in run():
close-if-open + await CLOSED before the claim fires (serialized on purpose —
parallel invites a mid-close race). App: _snap_to(0) on started (ceremony
played off-screen if backgrounded on partner panel); day-carry's deferred
re-open gated on _morning_active. Never caught in 5 sessions of device
testing because DevKit always reboots through Loader = clean stage.

FIX sealed-beats-clock (found via backwards clock travel). task_list gated
toggleability on the clock alone — a sealed TODAY rendered editable (server
held throughout; every tap 409'd). is_sealed threaded through _render /
_resolve_row_state: sealed renders settled + never toggleable regardless of
date; pre-payout mode explicitly wins (ceremony day is always already
sealed). App rest long-press bails on sealed. 4.5's MOTD-edit client half
must carry the same gate (tile noted).

FIX coin-source bound (index.ts). claim + resolve reject local_date outside
±2 days of server UTC (400 bad_date). Time travel stays legal and
self-spoiling; farming the ONLY coin source is closed. Sinks + day writes
deliberately unbounded — rationale recorded at LOCAL_DATE_BOUND_DAYS. W18.

FIX devkit wipe FK failure (found when D11's first real bug_reports row
broke the Default reseed — the bug proved the test). devkitWipeStatements()
shared helper: bug_reports / redemption_attempts / content_drop_deliveries
deleted BEFORE users; all three wipe sites (seedDefaultSpread,
fresh_install, recovery_flow) route through it. seed_devkit.sql synced.
Maintenance rule pinned: new users-FK table → add its delete there only.

RECORDED, not built:
  - Emoji glyphs don't render on work-laptop export (🔥/🟡 invisible incl.
    CoinFX flights, which fired correctly but invisibly; ★ fine). Suspect
    Godot 4.6.3 / export parity vs home PC — verify before any release
    build from this machine.
  - App local-date-change watcher (repaint + close now-future trays on
    backwards clock moves; forward already claim-handled) — designed,
    tiled 4.23.
  - Mid-ceremony KILL permanently loses today's MOTD pick (beat 9 never
    runs; no recovery surface until 4.5) — noted on 4.5. Server state
    stays coherent (claim atomic).
  - Sealed-grey cell renders UNMARKED, indistinguishable from untouched —
    [DECIDE] tiled 4.24.
  - Rapid-tick untick reproducible ONLY under mock-clock hopping (s29 #7
    residual) — PARKED.

DIRECTION LOCKED (end of session): walkback span-sealing amendment —
empty days adjudicated AT FIRST CONTACT during the walk (1-day gap + live
streak → rescue fires then, anchored to the gap day's own seal; 2+ day gaps
batch-seal grey silently; payout target seals after). Kills the discovered
wart: current adjacency checks behind the target only, so a streak advances
past a hole on day N and retroactively collapses on N+1. Stateless rescue
architecture survives unchanged. Tile 4.25 — doc before code, next session.

Files: morning_sequence.gd, App.gd, task_list.gd, index.ts, seed_devkit.sql,
DevKit.gd (rename). Client fixes device-verified within session; index.ts
deployed at close.

Session 31 time: ~4h.
Running total: ~156.75h.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION 32 — 2026-06-11 (mobile, at work — DOC-ONLY, no code)
4.25 DOC-LOCKED · stranded syncs written · wire pack · ★THEME PREP RESOLVED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PROJECT FILES RESET: clean-slate replacement (todo + devlog + .gd scripts
+ server zip). Verified all carry the s31 finals (five fix markers present)
— the s31 canonical-file trap is closed.

FOUND (repo audit before amending): NONE of the s31 bookkeeping syncs had
reached the repo, and the s30 morning-seq STREAK RESCUE GATE section was
never pushed at all (economy's s30 section landed; morning-seq header still
said s29). Pair-key §9.1a was the one s31 sync that DID land — that todo
item was simply unticked. Also: the s31 devkit.gd → DevKit.gd rename never
registered in git — Windows core.ignorecase swallows case-only renames, so
the repo still carries lowercase and a fresh clone resurrects the dead-
autoload-on-Android bug. Fix tiled: `git mv devkit.gd DevKit.gd` (laptop).

4.25 WALKBACK SPAN-SEALING — DOC-LOCKED (build owed, ~3-5h + verify).
Resolutions made inside the s31-locked direction, now authoritative in the
docs:
  - The claim walks the FULL span (last seal +1 → local_date −1), oldest
    first; every day ends terminal — contiguous history.
  - Never-opened days seal GREY: accounted_for stays 0, zero payout,
    SILENT (no ceremony for a day never engaged with). Accounted days seal
    at natural tier; the most recent is the payout target + ceremony day.
  - The walk runs even with NO payout target — the first-contact fix
    (skip Tue, open Wed: Tue judged Wed morning; no retroactive collapse).
  - Rescue = PRE-SCAN: exactly ONE zero-task day in the span AND streak ≥1
    → offer, anchored to that day's own seal; two-plus (incl. gap + grey
    target) → batch-seal silently, break, NO offer. Extends the locked
    no-false-hope rule — no chained offers, no two-purchase saves.
  - pending_rescue.target_date / target_tier become NULLABLE (rescue can
    fire on a payout-less claim). Pending seals NOTHING; resolve re-walks
    and seals the whole span in one batch. Stateless/re-entrant unchanged.
  - Shadowed accounted day (needs two consecutive claim failures): seals
    at tier WITH its payout, silently batched; ceremony presents only the
    most recent day.
  - Corrected on the way: the morning-seq doc's "stranded days drain
    most-recent-first, self-correcting" claim was WRONG in steady state
    (one unsealed row produced per day, one consumed — older rows shadowed
    forever, payout lost). Recorded as the second fault 4.25 retires.

FILES WRITTEN (7 — commit checklist in todo top block):
  - economy_redesign: streak rule → per-day walk-order math; rescue →
    first-contact + pre-scan; never-opened/shadowed earn rules; SPAN-
    SEALING STATUS line (doc describes target, wire runs s30 until 4.25).
  - morning_sequence: Q5 rewritten as span-sealing; STREAK RESCUE GATE
    section written fresh in 4.25 form (the missing s30 content); beats
    0/0b added to the beat list (s31 stage normalization synced); claim-
    contract step 5 fixed (still carried the SUPERSEDED s27 green-only
    streak rule); freeze/resume confirmed present; Q8 shipped-cost note +
    stale 300-600 pack price → 20k-100k; absence-return queue entry case.
  - system_map: FULL refresh — §4 to ten tables (content-drops trio), §5.1
    full endpoint surface (motd roll/pool/reroll, drops + ack, rest,
    resolve_solo were all missing), §5.2 FK-wipe helper, §5.3 ±2d bound +
    stranded-day fault + 4.25 forward note (observational discipline:
    shipped wire stays primary), §6-§7 client inventory REWRITTEN against
    the uploaded .gd set (the long-owed s24-31 debt paid; stale facts
    fixed: toggle_task signature, onboarding "STUB", Bonus "will apply",
    devkit menu), §10 sealed-beats-clock + bound invariants, §11/§12
    pruned of shipped items, repo path → C:\dev\, s30+s32 changelog rows
    (s30's refresh had never logged itself).
  - timezone_and_sealing: new CLOCK BOUNDS section (±2d coin-source bound
    + sealed-beats-clock client rule, generalised); anti-scrubbing section
    honestly QUALIFIED — the original "forward rolls skip, don't farm"
    analysis was wrong under fully-trusted local_date (roll-claim-roll-
    claim minted a payout per roll; the bound closes the mint); 4.25
    supersedes-on-build note on the single-row walkback; yesterday_iso
    DST edge recorded.
  - staggered_disclosure: week-mode cascade destination = Model B popup +
    one-time intro line (day-2 long-press relays into the feature itself;
    "tutorial" → "intro" throughout). 4.D2 fork recorded RESOLVED in todo;
    week-mode doc's own Model-B sync rides 4.D2's design pass.
  - wire_regression_pack.md (NEW): folds the s29/s30 session packs into
    one paste-ready reference ($BASE/$U session vars); W1-W17 marked
    PASSED s31; W18 commands; 4.25 re-spec block W13r/W14r (span shapes,
    shared sealed_at as the atomicity probe) + W19 (first-contact hole,
    null target) + W20 (gap+grey suppression); device rescue-affordability
    recipe. session_29/30_wire_tests.md DELETE on commit.
  - todo: top block → commit checklist + git-mv item; 4.25 → [~] design
    closed; 4.D2 fork resolved (Model B); doc-sync pile updated; s32
    total row.

THEME PREP DESIGN CONVERSATION — RESOLVED (same session, second
half; the critical-path conversation gating 4.14b/4.20/4.D, done
by mobile grilling against annotated screenshots + border sketch).
Theme doc REWRITTEN AS v2 (restructure supersedes the 2026-05-15
edition; locked v1 content carried, superseded models compressed
to history). The locks:
  - COLOUR SYSTEM: up to FOUR pixel-weighted roles per sticker
    (primary→accent by count; surface share stays the painter's);
    role-accent falls back to role-tertiary on 3-colour stickers;
    FREE NEUTRAL RAMP #000000/#5A5A5A/#B4B4B4/#FFFFFF — usable in
    stickers AND theme art, never retinted; exact grey rule:
    derivation excludes R==G==B exactly + alpha<1; AA neutral-to-
    neutral only; reference values locked #FF0000/#00FF00/
    #0000FF/#FF00FF. Supersedes strict-three-flats.
  - v1.0 SLOT CATALOGUE = SEVEN: leader, palette source,
    background, calendar card, tray card, popup skin, border.
    THE CELL STAYS SACRED — cell treatments (+ dead-cell, grid
    tile, flourish, label, effects, meta-mods) DEFERRED to v1.x
    with locked architecture preserved in the doc; audio already
    v1.1 globally. (Morgan: cells were the most daunting,
    smallest surface — deferring un-obscures the vision.)
  - NEW SLOTS: border = corner flourishes, z above background /
    below cards, never costs calendar width; convention = one
    border_corner.png auto-mirrored ×4 OR explicit _tl/_tr/_bl/
    _br subset (explicit wins). Popup skin = ONE 9-slice for all
    dialog surfaces. Calendar card + tray card = separate
    9-slice slots (clone-and-tweak economy). Tray buttons stock
    [LOCK AT FIRST ART].
  - RETINT MECHANISM LOCKED: PRE-BAKE at slot change (clarity
    over cleverness — legible GDScript, devtool-debuggable,
    transform-stack-free, preview = second bake cache). Shader
    recorded as fallback; indexed-PNG retired.
  - META-MODS reserved: palette transform stack between
    derivation and bake (jester permutes, yin-yang inverts,
    chrome hue-shifts; composable ordered list in
    active_theme.mods). v1.x feature, v1.0 pipeline hook.
  - DIMENSIONS derived from Layout.gd: background 360×800 @3x,
    card 9-slices 48×48/16px margins @3x, border corners 96×96
    @3x — all [TUNE AT FIRST ART]. Integer Nearest always.
  - R2 RUNTIME ASSET DELIVERY designed (closes the s29 gap):
    launch packs bundled res://; drops download to
    user://stickers/<id>/ on drop-ack; content_drop_assets rows
    ARE the manifest; assets immutable per sticker id (cache
    forever); PARTNER-RENDER RULE — client may fetch any
    referenced sticker's assets by id, entitlement gates use not
    pixels; offline degrades to stock + retry.
  - Picker: 30s preview timeout locked; locked stickers enticing
    not greyed; devtool gains accent swatch + neutral audit.
ART IS UNBLOCKED: per maximal pack = sticker 32×32 + background +
three 48×48 9-slices + 1-4 border corners. Sticker-only valid.

ALSO ADDED: tile 4.26 — notifications, CRUDE pass, wanted ASAP
for the daily-driver APK. Local only, no FCM. Design locked in
conversation: two daily notifications, tap opens app — morning
"your coins are waiting" + ~8pm "you haven't ticked your tasks
today, want to do that before bed?". Fixed times/copy v1.0,
customisable v1.x; true server push stays v1.x.

OPEN AFTER THIS SESSION: W18 wire check, W10 (optional), laptop-emoji
parity, git case-rename fix, commit the EIGHT files (seven above + theme
doc v2; checklist in todo). Then 4.25 build → daily-driver switch (with
4.26 + stamp pools), or straight to Aseprite — next roster-break call.

Session 32 time: ~6h (ESTIMATE — doc-only incl. THEME PREP; correct at
close).
Running total: ~162.75h.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION 33 — 2026-06-12 (laptop)
4.26 notifications BUILT · 4.25 SERVER SHIPPED + wire pass · bookkeeping cleared
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

BOOKKEEPING CLEARED (start of session):
  - s32 push verified on origin/main (fa78a24) — independently re-cloned and
    checked: wire pack present, session wire-test files deleted, theme v2 in.
    Todo commit block stripped.
  - DevKit.gd case rename committed + pushed (git mv on laptop; the
    core.ignorecase fresh-clone trap from s31/s32 is closed).
  - W18 PASSED: far-date claim → 400 bad_date, real date → 200. Proves the
    s31 dev deploy. (TestUser coins > lifetime on dev = SQL top-up artefact,
    not a bug — top-ups don't touch lifetime.)

TILE 4.26 — NOTIFICATIONS (crude/local): BUILT, DEVICE-PARTIAL. Time: ~3h
(ESTIMATE — correct at close).
  - Design doc NEW + AUTHORITY: four_tasks_notifications_design_notes.md.
    Morgan decisions locked: 6:30am morning / 8:00pm evening (fixed v1.0;
    constants are the v1.x customisation seam); lowercase copy drafted;
    permission requested at boot; suppression conditional everywhere; lapse
    decay daily×7 → gap grows +1/nudge (9,12,16,21,27,34) → weekly (41,48,
    55), horizon 56, both notifications fire on lapse days.
  - Plugin: godot-mobile-plugins/godot-notification-scheduler (Godot-4
    cengiz-pz lineage). Unified Android/iOS interface (iOS = Phase 5 export
    job), cancel-by-id, permission signals, reboot persistence, default
    icon shipped, auto-falls-back to inexact alarms (no scary permissions).
  - Gradle: Android Build Template installed + Use Gradle Build ON (laptop;
    ONE-TIME PER MACHINE — home PC needs the same click before its first
    gradle export). android/ stays git-ignored: editor-generated,
    version-tied, nothing hand-authored lives in it (earlier commit advice
    this session was wrong and withdrawn).
  - Notifications.gd autoload shipped — fully self-contained, ZERO edits to
    existing files (hooks State signals + FOCUS_IN directly). Model:
    re-derive, never remember — every relevant change cancels the
    deterministic id range (bases 1000/2000 + day offset, sweep 0-60) and
    reschedules the full forward window from State. Suppression is
    STRUCTURAL: day-0 morning is never scheduled at all (app open IS the
    claim); evening skips 4/4, rest day, past-8pm; mock clock = schedule
    nothing (DevKit clock games stay silent). Fingerprint-gated rebuilds
    keep repeat focus-ins free.
  - DEVICE: D-N1 (permission), D-N2 (evening fired 8:02 — inexact window,
    in spec), D-N3 (4/4 suppression), D-N4 (rest-day suppression) GREEN.
    D-N5/6/7 + swipe-kill (both phones) deferred to AMBIENT testing — leaf
    subsystem, nothing downstream depends on results; tracked in the todo
    open-tests block. Two of three self-test in normal daily-driver use.

TILE 4.25 — WALKBACK SPAN-SEALING, SERVER HALF: SHIPPED + FULLY
WIRE-VERIFIED. Time: ~2.5h (ESTIMATE — correct at close).
  - index.ts: single-target walkback + evaluateRescue + sealTarget REPLACED
    by buildSpan (anchor = last sealed day, or oldest row on fresh installs;
    strict date < local_date invariant preserved) + evaluateSpanRescue (the
    exactly-one pre-scan) + sealSpan (per-day streak walk oldest-first, one
    atomic batch, ONE shared sealed_at across every row it seals) +
    computeDayPayout (payout extracted per-day so target and shadowed
    accounted days share one implementation — same single-derivation
    principle as deriveTier).
  - Behaviour deltas vs s30/s31 wire: never-opened days get rows CREATED
    sealed grey accounted_for=0 (declined gaps included — old code left
    holes rowless); pending_rescue.target_date/tier nullable (first-contact
    hole); streak_at_stake = running streak AT the breaker (refinement:
    accounted partials earlier in the span advance it first); bought gap
    days write accounted_for=1 but never present as sealed_day.
  - Pre-flight before deploy: untouched regions verified byte-identical to
    the uploaded source; typecheck added zero new error kinds; pre-scan +
    streak walk extracted and run against all five wire scenarios in node.
  - WIRE (dev, all PASSED s33): W13r accept (streak 4, shared sealed_at,
    bought day purple/accounted=1, payout maths exact) + decline (grey row
    created accounted=0, streak 1, no charge); W14r (no offer at 2 holes,
    contiguous grey fill, streak 1); W19 decline (sealed_day null, streak 0,
    zero coin movement) + accept (sealed_day null, streak HELD 3, coins
    10,000, lifetime untouched); W20 (pre-scan suppresses at gap+grey, both
    seal grey one batch, streak 0); W11/W12 RE-PROVEN under span code (grey
    convert presents purple, streak held / decline presents grey, streak 0).
    Wire pack amended: W19/W20 seed SQL filled in (was "exact at build
    time"), rows marked passed.
  - Diagnostic worth keeping: claim-sealed rows share ONE sealed_at;
    seed-sealed rows step by exactly 86,400. Instant tell for "did the
    scenario reseed actually run".
  - REMAINS (pass 2): client deltas — rescue_confirm copy without the "and
    yesterday seals as X" framing when target is null; verify the
    coordinator's resolve → sealed_day:null path winds down with no
    ceremony. Then device pass.

DESIGN NOTE (no change needed): partner multiplier across absences —
already settled by the same-date-read model. Grey days have 0 base so the
multiplier question never arises; the first worked day back reads the
partner's REAL same-date completion. Nobody is disadvantaged by absence;
nothing to exploit.

Session 33 time: ~7h (ESTIMATE — correct at close; s32's ~6h estimate also
still owed its correction).
Running total: ~169.75h (BOTH estimates pending correction).
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━