docs: de-stale design notes against shipped v1.0 schema + system map

Audited all design docs against four_tasks_system_map.md and the
test matrix, corrected drift accumulated since the session-12 schema
rewrite. No content/design changes — only reconciling docs to what
shipped. System map + test matrix were already accurate; untouched.

Rebuilds (fix + trim):
- onboarding: join_by_values corrected to shipped shape (caller
  pre-exists with user_id, body {partner:{...}}, returns {pair_id},
  404 on no-match, no phantom-pair); recovery split to POST /resolve;
  added POST /users timing (end of screen 8); JAMIE -> Jamie
  (case-sensitive); cut session changelog blocks. 567 -> 368 lines.
- morning_sequence: dropped dead morning_payout_due_for mechanism
  (gone at S12) for the walkback/sealed_at gate; claim endpoint
  corrected to POST /users/:user_id/claim {local_date} -> {sealed_day,
  user}; days.diary -> days.motd; migration->rotation window; MOTD
  storage locked (today's days.motd, frozen at seal, header vs tray
  surfaces); Q7 partner reactions banner-flagged v1.x. 830 -> 236.
- rate_limiting: re-keyed pair_key -> user_id; dead /pair/:key URLs
  replaced with shipped /users/:user_id surface; trimmed. 332 -> 123.

Surgical (drift-only):
- week_mode: SCHEMA SUMMARY was WRONG vs shipped (seven booleans +
  (pair_key,name) overrides w/ task_1..4) -> bitmask + (user_id,
  weekday) PK + task_labels JSON, ISO weekday; status v1.x -> v1.0;
  corrected the misleading S12 changelog entry.
- theme: z-order layer 3 stamp -> sticker (cell has no stamp layer,
  stamp is on tray, S14); long-press reveal day-7 -> trigger-gated;
  migration_005 ALTER block -> shipped v1.0 schema; flagged the
  cell-overlay-bucket vs 9-state-ramp open question; migration ->
  rotation; write_rules xref repointed.
- architectural_preference: palette.tres example -> palette-from-PNG
  (the actual shipped decision); diary -> MOTD; migration -> rotation.
- staggered_disclosure: day-4 partner reactions flagged deferred-with-
  feature throughout; tile 0.4 -> tile 2.9 DevKit; partner_reactions
  xref noted as deferred/.
- stamp_tier: GET /pair/:key -> /users/:user_id; write_rules xrefs ->
  system map; tile 1.4 framing -> absorbed into 1.3.
- monetisation_position: migration_005/004 framing -> shipped v1.0
  schema; pair-key migration -> rotation.
- pair_key: added shipped-schema status note up top so the (accurate,
  historical) migration_005 mentions read as history not pending.
  Content was already correct.
- achievements_brainstorm, tracking, marketing: dead cross-ref
  filenames (pair_key_v2, write_rules), migration_00x framing, and a
  v1.x partner-reactions mention in launch copy corrected.

Superseded references removed/repointed throughout: write_rules
(retired S11), pair_key_v2 (never the on-disk name), migration_00x
(schema is monolithic since S12).

Not in this commit: repo index (stale, deferred), devlog AI reading
guide (still flagged in todo).